name = "eturkes/slide-gen"

version = "0.1.0"

license = "Apache-2.0 WITH LLVM-exception"

repository = "https://github.com/eturkes/slide-gen"

description = "Generate, validate, and render slide decks across sibling projects."

keywords = [ "slides", "codex", "cli" ]

preferred_target = "native"

import {
  "moonbitlang/async@0.20.0",
}
