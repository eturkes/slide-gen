# slide-gen — roadmap

Goal: a CLI + home for slides across `~/Projects/*`. Discover sibling projects → toggle on/off → `codex exec` generates a `deck.html`-style deck per enabled project → render to PDF. The repo is the deck home: `templates/deck.html` = style contract; `decks/<project>/` = generated output.

Stack: **MoonBit (native backend)** — the agent-oriented bet (AGENTS.md ethos). UX = flag subcommands (no TUI). State = gitignored local. Discovery = all sibling dirs, default-off. (Owner-confirmed at M2 plan.)

Seed: commit `0526d53` (repo init: deck.html, AGENTS/CLAUDE, `.agent` boilerplate) + the `templates/deck.html` move. `deck.html` = the content-stripped generalization of the proven prototype `~/Projects/spine-segmentation/docs/slides/` (filled deck.html + figures.py + render.py → PDF) — M2/M3 mirror it (mechanics in memory.md).

## Legend
- milestone status ∈ {UNPLANNED, IN-PROGRESS (units enumerated), IMPLEMENTED (units DONE, unreviewed), REVIEWED}.
- unit status ∈ {OPEN, DONE}; `ctx:` = context-usage recorded at unit close (WORK-UNIT only).
- commit trace keys: unit `(M<m>.<u>)`, plan `(M<m> plan)`, review `(M<m> review)`. History: `git log --grep "(M2[. ]"`.
- **DONE units keep a one-line marker + ctx only** — full as-built / codex-review detail lives in the code + `git log` (code = source of truth; don't restate it here). Durable cross-cutting lessons live in memory.md.

## Milestone ledger
- **M1 CLI foundation** — discovery + toggle state (no codex, no render). STATUS: **REVIEWED**.
- **M2 Generation** — `codex exec` per project → template-styled deck BUNDLE (`deck.html` + `provenance.json` + `gaps.md` + `figures.py` + `assets/*.png`), gated by a deterministic MoonBit validator. STATUS: **IN-PROGRESS** (15 units; detail below).
- **M3 Rendering** — `deck.html` → per-page PNG → PDF@72dpi; mirror prototype `render.py` (chromiumfish + raster, section count from DOM). STATUS: UNPLANNED.
- **M4 Integration / packaging / docs** — end-to-end `run`, batch over enabled set, distribution, README, quality gates. STATUS: UNPLANNED.

Plan each milestone in its own session when it becomes active (M3–M4 deferred per owner). M2's parked decisions were resolved at its plan (furigana · sandbox · input scope · output commit · codex-trust — see M2 §Decisions). Still parked for later planning: render DPI + raster-vs-vector (M3); end-to-end `run` + batch + distribution (M4).

---

## M1 — CLI foundation (REVIEWED)
Buildable MoonBit-native CLI: `cli.mbt` (`@argparse` command tree), `discovery.mbt` (sibling readdir + symlink-aware `@fs.kind`; skip dotfile/self/unrepresentable-name; codepoint sort), `state.mbt` (`Set[String]` toggle ↔ newline `enabled.txt`; atomic temp+rename; `moon.mod`-declared repo-root anchor), thin `cmd/main`. Discovers sibling dirs under the repo parent; subcommands `list|status|enable|disable|toggle`, run-from-anywhere; errors → stderr + nonzero exit (2 parse, 1 runtime). Gates via `justfile` (fmt · check · build --target native · test).
Units (all DONE): **M1.1** toolchain + scaffold (ctx 69%) · **M1.2** discovery (ctx 87%) · **M1.3** toggle state + subcommands (ctx 88%). Review: 14 tests 0-warn, real-repo smoke lists sorted siblings; repo-root anchor validates a `moon.mod` declaring `eturkes/slide-gen` (walks past a foreign ancestor). Accepted as-is (single-user local, not remotely exploitable): `parse_state` never validates on read; fixed predictable temp `enabled.txt.tmp`. Detail: code + `git log --grep "(M1[. ]"`. MoonBit/API lessons → memory.md.

---

## M2 — Generation (IN-PROGRESS)

As-designed: `slide-gen generate [<project>]` drives `codex exec` (one shot per project) to author a deck BUNDLE at `decks/<project>/` — `deck.html` (template-styled) + `provenance.json` (per-claim source citations) + `gaps.md` (unverifiable / human-needed) + `figures.py` + `assets/*.png`. A deterministic MoonBit VALIDATOR gates every bundle (template conformance + provenance quote-grep + furigana structural guards); a project-local Python analyzer cross-checks furigana readings (hybrid). Deck CONTENT = codex's job (nondeterministic, env-gated + external `timeout`); the harness + validator are the testable MoonBit surface.

### Decisions (owner + research, at plan; env/tool mechanics → memory.md)
- **Gate PASSED** — `codex exec` with an explicit `--sandbox` IGNORES the interactive trust gate (probe-verified: read-only exec on a non-trusted sibling → exit 0, replied); `trust_level` only gates project-local `.codex/` loading, not runtime.
- **Sandbox** = `--dangerously-bypass-approvals-and-sandbox` (owner; the container is externally sandboxed). Harness must still direct writes to the bundle dir (bypass = no write confinement).
- **Input scope** = codex may read ANYTHING (owner; no deny-list / no input curation). The provenance **quote-grep** anti-hallucination check stays independent of that.
- **Furigana** = HYBRID — codex writes the three tiers incl. furigana → MoonBit structural guards (`.jp`==rt-stripped-`.jpf` · `<rt>` all-kana · ruby covers kanji runs) → Python analyzer (fugashi+UniDic / SudachiPy) independently re-reads the JA; disagreements / analyzer-unknowns → `gaps.md`.
- **Output** = COMMITTED deck home — track `decks/<project>/{deck.html,provenance.json,gaps.md,figures.py,assets/*.png}`; gitignore only render intermediates (`_deck_full.png`, `page_*.png`). **Privacy**: the no-secrets/PHI rule covers the WHOLE committed bundle (quote-grep COPIES verbatim source text into `provenance.json`, so a cited secret would leak). No deterministic secret gate (owner chose read-anything + committed) → prompt guideline + human review before the owner pushes (owner handles remote).
- **Provenance contract**: `provenance.json` rows `{id,slide,claim,status:verified|inferred|gap,src:"path:Lstart-Lend" (parent-relative or absolute),quote:"<verbatim>"}`; deck claims carry `data-claim="id"`. The quote must LITERALLY occur WITHIN the cited `src` line-slice (a cheap EXISTENCE check — kills invented quotes; NOT a truth check — misattribution / cherry-pick still pass → analyzer + human). A row must NOT cite a generated bundle file (no self-citation). Prompt drives a codex DRAFT→re-verify-quotes+line-numbers→FINAL self-critic.
- **Section-count gotcha**: count `<section class="slide">` after COMMENT-STRIP — the template's line-8 header comment inflates a raw `grep -c` by 1.
- **Figures**: codex authors `figures.py` from COMMITTED, CITED numbers and runs it (bypass permits exec) → `assets/*.png`; raw-data or failed figures → a `.fig-ph` placeholder + a `gaps.md` entry (human runs it, à la the prototype's `figure_example.py`). Each committed `assets/*.png` earns a `provenance.json` row citing its `figures.py` + the cited numbers.

### Hardening backlog (validate.mbt quote-safety; non-blocking, UNSCHEDULED — NOT a unit)
The pre-cleaners `strip_comments`/`strip_raw_text` run BEFORE discovery and stay quote-UNAWARE → a `<!--`/`<style>`-shaped substring inside a quoted attr value (`data-note="<!--"`) can mis-trigger stripping and BLANK real markup (fail-open). Discovery itself (`find_all_open_tags`/`find_by_class`/`find_data_claims`) is now quote-safe, so this pre-clean pass is the SOLE residual. Because the M2.2b deck cross-ref consumes the cleaned doc, this can HIDE a fabricated/dangling `data-claim` (a concrete anti-hallucination stake, bounded by the semi-trusted-codex assumption). Fix = ONE unified quote+context-aware tokenizer (comment + raw-text + tag boundaries in one pass) replacing both pre-cleaners (a naive quote-aware `strip_comments` would corrupt `<script>if(a<b)…`). Revisit at M2 review or if deck inputs become less trusted.

### Units

**M2.1 — deterministic deck VALIDATOR (`validate.mbt`)** [DONE]. Three vertical slices by check-group over one growing `pub validate_deck(html,slug,assets)->Report` (`Report` = MUST/SHOULD `Finding`s), each self-testing at ~half a window: **M2.1a** envelope + light string-scan primitives + image checks (ctx 81%) · **M2.1b-i** slide structure + count checks + heavy discovery machinery (`scan_deck`/`find_by_class`/`Elem`) (ctx 64%) · **M2.1b-ii** slide text checks + entity-decode (ctx 77%) · **M2.1c** entry tier-model + furigana guards (ctx 86%). No stdlib HTML parser → targeted string-scan in codepoint space on a once-cleaned doc (comment-strip THEN raw-text-blank). Reusable primitives + furigana/state-machine/0-warn lessons → memory.md. Detail: code + `git log --grep "(M2.1"`.

**M2.2 — provenance/gaps VALIDATOR (`provenance.mbt`, same package, reuses validate.mbt primitives)** [DONE]. Defines + enforces the provenance contract above. Four slices: **M2.2a-i** row-schema + gaps disclosure + async `validate_bundle_dir` shell (ctx 61%) · **M2.2a-ii** source-ref parse + quote-grep (`parse_src_ref`/`line_slice`/`resolve_src`/`is_inside`/async `check_row`) (ctx 71%) · **M2.2b-i** deck dangling-claim cross-ref MUST (`find_data_claims`; `check_provenance` returns the schema-valid id set) (ctx 70%) · **M2.2b-ii** coverage SHOULD warnings (untagged-number, uncited-asset; introduces `should()` + `Elem.open_lt`) (ctx 74%). Src-helper + cross-ref lessons + the path-norm RESIDUAL (fixed in M2.3b-i) → memory.md. Detail: code + `git log --grep "(M2.2"`.

**M2.3 — codex harness.** Shared design (every slice honors): codex's working root `-C cwd` = repo-parent = `split_path(root).0` = the provenance `src_base`, so sibling sources cite parent-relative (no `../`); the prompt rides STDIN (never argv); the whole invocation wraps `timeout --kill-after=30 <secs>` (hard-kill a SIGTERM-ignoring run; `timeout 0` DISABLES the kill → the ceiling must stay positive); the bundle stages under the reserved dot-dir `decks/.partial/<project>` (NOT `decks/<project>.partial`, which a sibling named `<project>.partial` would own — `discover` skips leading-dot names, so the dot-dir is collision-free) and validates via `validate_bundle_dir(stage, project, src_base=cwd)`.

- **M2.3a Codex invocation + `generate` CLI + dry-run** [DONE] — the DETERMINISTIC half (no codex, no async, no new imports). `generate.mbt`: `pub let default_timeout_secs=1800`; `priv struct GenPlan{project,cwd,source_dir,template,stage_dir,deck_dir}`; `plan_generate(root,project)` (pure path arithmetic; `join_path` is `//`-safe, fs-root special-cased); `pub build_codex_argv(cwd, timeout_secs?=default)` = `["timeout","--kill-after=30",<secs>,"codex","exec","--dangerously-bypass-approvals-and-sandbox","--skip-git-repo-check","-C",cwd]` (prompt on STDIN, not argv); `build_prompt(plan)` (first-draft `$|` paren-wrapped multiline; M2.4 refines); `pub render_dry_run(root,project,timeout_secs?)` (paths + argv-line + stdin prompt; PURE → golden-tested; `join_argv`/`shell_quote` for display quoting). cli.mbt +`generate` subcommand (`--dry-run` flag + shared name positional). main.mbt dispatch: reject a non-candidate FIRST (exit 1), then `--dry-run` → `@stdio.stdout.write(render_dry_run(...))`, else a forward-stub `die` (M2.3b-iii replaces). 83 tests, 0-warn. ctx 88%. Detail: code + `git log --grep "(M2.3a"`.

- **M2.3b-i Provenance path-normalization + timeout-ceiling guard** [OPEN] — the LIGHTEST, gate-independent slice; a promote-safety BLOCKER (codex-review-2 MAJOR): a generated deck can cite an absolute/`..`-escaped path OUTSIDE src_base, OR `..`-re-enter its own bundle past the string-prefix `is_inside` self-citation guard, defeating quote-grep anti-hallucination (memory §PROVENANCE-src-helpers RESIDUAL). Restate: normalize `..`/`.` in the provenance `src` BEFORE the confinement + self-citation checks, and add src_base confinement. FIX:
  - `provenance.mbt` — NEW pure `fn normalize_path(path : String) -> String`: fold `.`/`..` segments in codepoint space (`String::split("/")`), preserving leading-`/` absoluteness (`..` pops a prior non-`..` segment; at an absolute root a leading `..` is dropped, a relative leading `..` is kept so an escape stays visible). Idempotent on already-canonical paths.
  - `provenance.mbt` `check_row` — replace `let resolved = resolve_src(path, src_base)` with `let resolved = normalize_path(resolve_src(path, src_base))`; run in THIS order (self-citation now tested on the NORMALIZED path so `..`-reentry no longer slips; self-citation BEFORE confinement because bundle_dir ⊂ src_base): `if is_inside(resolved, normalize_path(bundle_dir)) { push must("prov.self_citation", "row \{r.id}: …") ; return }` → NEW confinement `if !is_inside(resolved, normalize_path(src_base)) { push must("prov.src_outside", "row \{r.id}: src escapes the source base") ; return }` → existing exists / `@fs.kind`==Regular / read / `line_slice` / quote checks.
  - `generate.mbt` `build_codex_argv` — clamp a non-positive ceiling: `let secs = if timeout_secs <= 0 { default_timeout_secs } else { timeout_secs }` before the argv (keeps the SIGKILL ceiling positive).
  - New code: `prov.src_outside` (MUST). Tests — whitebox `normalize_path` (`a/./b`→`a/b`, `a/b/../../c`→`c`, `/x/../y`→`/y`, `../../etc`→`../../etc`, `./a`→`a`); provenance (reuse M2.2a-ii's tmpdir + src-file harness): a `..`-reenter-bundle citation → `prov.self_citation`; an absolute path outside src_base → `prov.src_outside`; a `../`-escape outside src_base → `prov.src_outside`; an in-src_base citation still validates (existing a-ii clean / self-citation / src_missing / src_range tests stay green — normalize is idempotent on them, so reconcile only if a fixture path isn't already canonical); `build_codex_argv` with `timeout_secs=0` and negative → the default in argv. Accept: each seeded escape → its code; in-base citations still pass; ceiling stays positive; `just fmt/check/build/test` 0-warn; no codex.

- **M2.3b-ii Stage / validate / promote / retry orchestration (stub-bundle tests)** [OPEN] — gate-independent (no live codex — a producer thunk is injected; codex enters only in b-iii). NEW in `generate.mbt`:
  - `pub(all) enum GenOutcome { Promoted ; Retained(Report) ; ExecFailed(String) }` (main + tests build variants → `pub(all)`, else 4036).
  - `async fn stage_validate_promote(plan : GenPlan, produce~ : async (GenPlan) -> Int, retries~ : Int = 1) -> GenOutcome` — the injected producer returns codex's exit code (prod = `run_codex`, b-iii; tests = a closure that drops stub files then returns 0/nonzero). CONFIRM MoonBit's async-closure param spelling on landing (`async (T) -> R`); the banked DESIGN, not the syntax, is the seed. Per attempt: recreate an empty `plan.stage_dir` (rmdir-if-exists via `@fs.exists`/`@fs.rmdir(recursive=true)`, then mkdir parents); `let code = produce(plan)`; guard `code == 0` else `ExecFailed`; guard `plan.stage_dir/deck.html` exists ∧ non-empty else a retain-candidate; `let report = validate_bundle_dir(plan.stage_dir, plan.project, plan.cwd)`; if `report.ok()` → PROMOTE (remove an existing `plan.deck_dir`, then `@fs.rename(stage_dir, deck_dir)` — dir-rename replace semantics are unverified, so the unit confirms `rename` vs rmdir+rename) → `Promoted`; else `Retained(report)`. A non-Promoted outcome retries up to `retries` (fresh stage each time); a retained/failed stage is LEFT for inspection (not deleted).
  - Tests — blackbox `_test.mbt` (tmpdir harness like validate_test.mbt's `with_task_group`+`tmpdir`+`add_defer`; build the `GenPlan` against tmpdir roots): a producer dropping a GOOD stub bundle (reuse `envelope()`/`entry()` + a gap-row `provenance.json` + a `no gaps` gaps.md) → `Promoted` and `deck_dir/deck.html` exists; a producer dropping a BAD bundle (no deck.html, or a dangling `data-claim`) → `Retained` and `deck_dir` absent + stage retained; a producer returning nonzero → `ExecFailed`; a producer failing the first attempt then dropping a good bundle → `Promoted` (retry proven). Accept: stub bundles drive promote-on-pass / retain-on-fail / retry-once; `just` 0-warn; no codex.

- **M2.3b-iii Live codex exec + CLI wiring + gitignore** [OPEN, live-codex spend — GATED] — the real producer + glue; needs its gate (live codex) met. NEW in `generate.mbt`:
  - `async fn run_codex(plan : GenPlan) -> Int` = `let argv = build_codex_argv(plan.cwd) ; let (code, _out, _err) = @process.collect_output(argv[0], argv[1:], stdin=build_prompt(plan), cwd=plan.cwd) ; code` (prompt on stdin; the `timeout` wrapper is already argv[0..2]).
  - `pub async fn generate_project(root : String, project : String) -> GenOutcome` = `stage_validate_promote(plan_generate(root, project), produce=run_codex)`; then POST-RUN write-confinement — `@process` `git status --porcelain` over the slide-gen repo, assert nothing outside `decks/` changed (a dirtied source under bypass invalidates the run + closes quote-injection); a dirty tree → `ExecFailed`.
  - main.mbt: replace the M2.3a forward-stub `die(...)` arm with `generate_project(root, target)` → `Promoted` = exit 0 + a manifest line; `Retained(r)` = stderr `r.render()` + exit 1; `ExecFailed(m)` = stderr + exit 1. (Explicit target only; the no-arg enabled-set batch is M2.6.)
  - `.gitignore`: drop the `decks/` ignore so bundles commit; keep render-intermediate ignores narrow (deferred to M3); verify committed `assets/*.png` stay tracked via `git check-ignore`.
  - Tests: a `SLIDE_GEN_LIVE`-gated (mirrors M1.3's `SLIDE_GEN_BIN`) live smoke that INVOKES codex + lands a bundle (validator PASS NOT required — the first passing bundle is M2.4); bare `moon test` early-returns when unset. Accept: gated live run invokes codex + stages a bundle; CLI maps promote/retain/fail to exit codes; `decks/` tracked (check-ignore); `just` 0-warn.

- **M2.4 Prompt contract + live refinement** [OPEN, live-codex spend] — author `templates/deck-prompt.md` (loaded at runtime): embed `templates/deck.html` as the style contract; three-tier entries; INTRODUCE `data-claim` attrs into the template + prompt few-shot (the template lacks them today — this unit adds that forward contract); the `provenance.json`+`data-claim`+`gaps.md` contract with ONE verified-row + ONE gap-row few-shot; DRAFT→re-verify-quotes+line-numbers→FINAL self-critic; figure policy (author + run `figures.py` from committed cited numbers → `assets/*.png` + a citing provenance row; raw-data → placeholder+gaps); write the bundle to fixed paths, final message = a manifest; keep secrets/PHI out of the WHOLE bundle (content guideline — owner allows READING anything, but committed deck/provenance/gaps/assets must not dump creds/PHI). This unit lands the FIRST validator-PASSING live bundle. Refine against real codex runs on `spine-segmentation` (richest input + a hand-authored reference deck to compare) then `rehab` (committed aggregate numbers → a real figure) until the output passes the M2.1+M2.2 validator + emits an honest `gaps.md` (real gaps or the sentinel) — if refinement runs hot, close after the first project + carry the second to a follow-up unit. Accept: a live run yields a validator-passing bundle with honest gaps; the prompt is version-controlled + documented. GPT-5.6 Sol prompt-guidance adherence (developers.openai.com/api/docs/guides/prompt-guidance-gpt-5p6): short outcome-first scaffolding (Role/Goal/Success-criteria/Constraints/Tools/Output/Stop-rules, each short); reserve absolutes (always/never/must/only) for TRUE invariants (never-invent quotes/line-nums/readings · secrets+PHI out · cite-only-files-actually-read · deck data-claim↔provenance id-parity), decision-rules for judgment calls; explicit STOP rules so loop-minimization never outranks citation/evidence (missing fact → NAME it + route gaps.md, author no guess); ground claims verified|inferred|gap (= provenance.status; absence-of-evidence ≠ factual 'no'); self-validate (quote-in-slice · id-parity · furigana guards · visual render check) BEFORE the manifest; autonomy = in-scope bundle writes only (host sandboxed, no approval pauses). Accept(+): the authored prompt demonstrably adheres to that guidance.

- **M2.5 Furigana analyzer cross-check** [OPEN] — project-local Python helper (`tools/furigana_check.py`, uv-managed; authoring-time only, never in the MoonBit binary): read the deck's `.jp`/`.jpf`, run fugashi+UniDic (or SudachiPy — decide at impl; UniDic ≈ best analyzer CER) to independently read the JA, compare vs codex's furigana; agreements ok, disagreements / analyzer-unknowns → append structured `gaps.md` entries (term · codex-reading · analyzer-reading · human-review). Wire post-codex in the generate pipeline. Tests: on a SMALL controlled JA fixture (common words the analyzer reads reliably) the helper flags a deliberately-wrong reading and passes a correct one; the spine reference deck is an INTEGRATION case where genuine analyzer disagreements (technical / proper-noun / loanword vocab) route to structured `gaps.md` entries, not test failures. Accept: flags a wrong reading + passes a correct one on the controlled fixture; spine disagreements surface as gaps; wired; authoring-time only.

- **M2.6 Batch over enabled set + robustness** [OPEN] — `generate` with no arg → every enabled project (from state); per-project failure isolation (one failure ≠ abort the batch); a summary report; retry-once; timeout / partial-output handling; idempotent regenerate (overwrite). Tests: batch dispatch over a fixture enabled-set (dry-run, no live codex); failure-isolation behavior. Accept: processes every enabled project, isolates failures, prints a summary; a gated live run over ≥2 enabled projects lands validated bundles.

Sequence: M2.1a→b-i→b-ii→c→M2.2a-i→a-ii→b-i→b-ii (pure/async validators, no codex) → M2.3a (invocation + dry-run + CLI, deterministic) → M2.3b-i (path-norm + timeout ceiling) → M2.3b-ii (stage/validate/promote/retry, stub bundles) → M2.3b-iii (live exec + CLI + gitignore; live smoke env-gated) → M2.4 (prompt + FIRST passing live bundle) → M2.5 (furigana analyzer) → M2.6 (batch). Gates: extend the justfile (existing `fmt`/`check`/`build`/`test` + the Python tool's lint/test + the env-gated live-codex test).
