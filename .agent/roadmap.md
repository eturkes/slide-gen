# slide-gen — roadmap

Goal: a CLI + home for slides across `~/Projects/*`. Discover sibling projects → toggle on/off → `codex exec` generates a `deck.html`-style deck per enabled project → render to PDF. The repo is the deck home: `templates/deck.html` = style contract; `decks/<project>/` = generated output (later milestones).

Stack: **MoonBit (native backend)** — the agent-oriented bet (AGENTS.md ethos). UX = flag subcommands (no TUI). State = gitignored local. Discovery = all sibling dirs, default-off. (Decisions confirmed by owner this planning session.)

Seed: commit `0526d53` (repo init: deck.html, AGENTS/CLAUDE, `.agent` boilerplate) + this session's `templates/deck.html` move. `deck.html` is the content-stripped generalization of the **proven prototype** `~/Projects/spine-segmentation/docs/slides/` (filled deck.html + figures.py + render.py → PDF) — M2/M3 mirror it (mechanics in `memory.md`).

## Legend
- status ∈ {UNPLANNED, IN-PROGRESS (units enumerated), IMPLEMENTED (units DONE, unreviewed), REVIEWED}.
- unit status ∈ {OPEN, DONE}; `ctx:` = context-usage recorded at unit close (WORK-UNIT only).
- commit trace keys: unit `(M<m>.<u>)`, plan `(M<m> plan)`, review `(M<m> review)`. History: `git log --grep "(M1[. ]"`.

## Milestone ledger
- **M1 CLI foundation** — discovery + toggle state (no codex, no render). STATUS: **REVIEWED**.
- **M2 Generation** — `codex exec` per project (batch = enabled set; explicit target = any candidate) → template-styled `deck.html` (+ figures, provenance/gaps, furigana policy). STATUS: **IN-PROGRESS** (11 units; M2.2 = M2.2a-i/a-ii/b — M2.2a further split into SCHEMA+gaps+shell (a-i) vs SOURCE-ref+quote-grep (a-ii) after its full `provenance.mbt` typechecked 0-warn yet the test-write overran, a 4th impl-clean-tests-overrun; each slice now self-tests at ~half a window).
- **M3 Rendering** — `deck.html` → per-page PNG → PDF@72dpi; mirror prototype `render.py` (chromiumfish + raster, section count from DOM). STATUS: UNPLANNED.
- **M4 Integration / packaging / docs** — end-to-end `run`, batch over enabled set, distribution, README, quality gates. STATUS: UNPLANNED.

Plan each milestone in its own session when it becomes active (M3–M4 deferred per owner). M2's parked decisions were all resolved at its plan (furigana · sandbox · input scope · output commit · codex-trust) — see the M2 section. Still parked for later planning: render DSF/DPI + raster-vs-vector (M3); end-to-end `run` + batch-over-enabled + distribution (M4).

---

## M1 — CLI foundation (reviewed)

As-built (code = source of truth; MoonBit mechanics/gotchas → memory.md): a buildable MoonBit-native CLI — `cli.mbt` (`@argparse` command tree), `discovery.mbt` (readdir + full-path symlink-following `@fs.kind`; skip dotfile/self/unrepresentable-name; codepoint sort), `state.mbt` (`Set[String]` toggle ↔ newline `enabled.txt`, atomic temp+rename; module-validated `moon.mod` repo-root anchor), thin `cmd/main`. Discovers sibling dirs under the repo's parent; subcommands `list|status|enable|disable|toggle`, run-from-anywhere; errors → stderr + nonzero exit (2 parse, 1 runtime). Gates `moon fmt · check · build --target native · test` via `justfile`.

Units (≤200K window each):
- **M1.1 Toolchain + scaffold** [DONE] — MoonBit installed (`moon 0.1.20260618`, `moonc v0.10.1`); module `eturkes/slide-gen`, DSL format (`moon.mod` preferred_target=native + `async@0.20.0` pinned; `moon.pkg`), `cmd/main` layout (root lib pkg `cli.mbt`: `pub fn cli() -> @argparse.Command`; thin `cmd/main` `is-main` entry). `justfile` gates (fmt/check/build/test/all/run) prepend `~/.moon/bin`; `.gitignore` seeded (`_build/` `target/` `.mooncakes/` `.moonagent/` `enabled.txt` `decks/`). Main is sync `fn main` (async deferred to M1.2 per Async-from-start note). **Accept met**: fmt/check clean (0 warn), native ELF built, `slide-gen --help` renders via argparse, `moon test` passes (1 golden). ctx: 69% 138K/200K
- **M1.2 Discovery** [DONE] — sibling-enumeration module (readdir + full-path kind, symlink-aware, no recurse, exclude self; parent = repo-parent). **Tests**: tempdir fixtures (real dir / symlink→dir / symlink→file / broken symlink / plain file / dotfile-dir / nested subdir / cwd ≠ scanned parent) → assert exact candidate set. **Accept**: correct set on fixtures; `list` shows all (default-off). ctx: 87% 174K/200K
- **M1.3 Toggle state + subcommands** [DONE] — `Set[String]` model; newline file read (split on `\n`, strip only the line terminator + drop empty lines — do NOT trim name content: legal POSIX names can hold leading/trailing spaces) + write (sorted, atomic). Name policy: at discovery skip names with `\n`/`/`/control chars or leading/trailing whitespace (unrepresentable in the line format) → serialize stays total over candidates. `list/enable/disable/toggle/status`; anchor discovery on the repo root (validate cwd = repo, or walk up to the `moon.mod` marker) so commands work run-from-anywhere, not only cwd=repo (M1.2 `list` was cwd-relative); `enable`/`toggle`-on validate name ∈ candidates; `disable` (and `toggle`-off of a stale entry) + `status` also handle STALE names (project deleted/renamed post-enable) → `disable <stale>` removes it, `status` flags entries ∉ candidates. Errors (unknown name on `enable`/`toggle`, bad args, `@env.current_dir`=None) → message to stderr + nonzero exit (`parse` raises; `runtime_exit_native` non-public → pick an exit primitive, e.g. `@process`/core); success exits 0. **Tests**: `@quickcheck` sample-gen roundtrip serialize→parse→eq with a **constrained** name generator matching that policy (real finding: unconstrained `String` breaks the newline format); blackbox `list`-stdout + enable/disable/toggle/unknown-name/stale-disable/persist-across-invocations + exit-status assertions (0 success, nonzero on unknown + bad-arg); `inspect` goldens on serialize + `list` output. **Accept**: state persists; subcommands mutate correctly; unknown rejected with nonzero exit; stale removable; sample + blackbox + gates pass. Deviations from locked design: FIXED temp `enabled.txt.tmp` (not unique — single-user last-writer-wins) · property via `@splitmix` RNG direct (not `@quickcheck.samples`) · exit via libc `c_exit` FFI · repo-root marker `moon.mod` (module-validated in M1 review) · e2e gated on `SLIDE_GEN_BIN`. ctx: 88% 176K/200K

## M1 review outcome (REVIEWED)
Gates green (14 tests, 0 warn); real-repo smoke lists sorted siblings. Deferral (a) RESOLVED: repo-root anchor now validates `moon.mod` declares `eturkes/slide-gen` (`is_repo_marker`) → the binary walks past a foreign ancestor `moon.mod` instead of mis-anchoring, and the "not inside a slide-gen project" error is now honest (e2e plants a foreign `moon.mod` to prove the skip; whitebox test pins the match). Decision-table test made exhaustive (12th triple `Enable,in_set,!candidate → Reject`). Accepted as-is (single-user, local, not remotely exploitable): (b) `parse_state` never validates on read → an externally-tampered unrepresentable line loads unsanitized as a stale entry (`status` prints it raw; `disable` still removes most such names once typed; a future `prune` would sanitize on read — deferred until state gains structure); (c) fixed predictable temp `enabled.txt.tmp` — concurrent writers / pre-planted-symlink out of scope. Async-churn / native-cc / no-MoonBit-LSP watch items → memory.md.

---

## M2 — Generation (planned)

As-designed: `slide-gen generate [<project>]` drives `codex exec` (one shot per project) to author a deck BUNDLE at `decks/<project>/` — `deck.html` (template-styled) + `provenance.json` (per-claim source citations) + `gaps.md` (unverifiable / human-needed) + `figures.py` + `assets/*.png`. A deterministic MoonBit VALIDATOR gates every bundle (template conformance + provenance quote-grep + furigana structural guards); a project-local Python analyzer cross-checks furigana readings (hybrid). Deck CONTENT = codex's job (nondeterministic, gated behind an env flag + external `timeout`); the harness + validator are the testable MoonBit surface.

Resolved this plan (probe + research workflow + owner). Env/tool mechanics (codex exec flags, furigana tooling) → memory.md; here = decisions:
- **Gate PASSED** — `codex exec` with an explicit `--sandbox` IGNORES the interactive trust gate (probe: read-only exec on non-trusted `uas` → exit 0, replied). The "20 non-trusted projects" worry is moot; `trust_level` only gates project-local `.codex/` loading, not runtime.
- **Sandbox** = bypass (`--dangerously-bypass-approvals-and-sandbox`) — owner; the container is externally sandboxed. Harness must still direct writes to the bundle dir (bypass = no write confinement).
- **Input scope** = codex may read ANYTHING — owner (overrode both curated options); no deny-list / no input curation. Independent of that, the provenance **quote-grep** anti-hallucination check stays.
- **Furigana** = HYBRID — owner: codex writes the three tiers incl. furigana → MoonBit structural guards (`.jp`==rt-stripped-`.jpf` · `<rt>` all-kana · ruby covers kanji runs) → Python analyzer (fugashi+UniDic / SudachiPy) independently re-reads the JA; disagreements / analyzer-unknowns → `gaps.md` human-review.
- **Output** = COMMITTED deck home — owner: track `decks/<project>/{deck.html,provenance.json,gaps.md,figures.py,assets/*.png}`; gitignore only render intermediates (`_deck_full.png`, `page_*.png`). **Privacy**: the no-secrets/PHI rule covers the WHOLE committed bundle (deck + provenance quotes + gaps + figures + assets), not just the deck — quote-grep COPIES verbatim source text into `provenance.json`, so a cited secret would leak. No deterministic secret gate (owner chose read-anything + committed) → prompt guideline + human review before the owner pushes (owner handles remote).
- **Provenance contract** (research): `provenance.json` rows `{id,slide,claim,status:verified|inferred|gap,src:"path:Lstart-Lend" (path = parent-dir-relative or absolute),quote:"<verbatim>"}`; deck claims carry `data-claim="id"`. The quote must LITERALLY occur WITHIN the cited `src` line-slice — BOTH MUST (quote-in-slice, not merely quote-in-file). This is a cheap EXISTENCE check (kills invented quotes), NOT a truth check — misattribution / cherry-pick / wrong-inference still pass (→ analyzer + human). A row must NOT cite a generated bundle file (no self-citation). Prompt drives a codex self-critic DRAFT→re-verify-quotes+line-numbers→FINAL.
- **Section-count gotcha** (memory, re-confirmed): count `<section class="slide">` after COMMENT-STRIP — the template's line-8 header comment inflates a raw `grep -c` by 1.
- **Figures**: codex authors `figures.py` from COMMITTED, CITED numbers and runs it (bypass permits exec) → `assets/*.png`; raw-data or failed figures → a `.fig-ph` placeholder + a `gaps.md` entry (human runs it, à la the prototype's `figure_example.py`). Each committed `assets/*.png` earns a `provenance.json` row citing its `figures.py` + the cited numbers (assets are provenance-covered too).

Units (≤200K each; gate-independent deterministic units first, then the live-codex ones):
M2.1 = the deterministic deck VALIDATOR (`validate.mbt`), split into THREE vertical slices by CHECK GROUP — envelope | slide-skeleton (units b-i/b-ii) | entry-furigana. Each slice COMPLETES + TESTS one growing public `validate_deck` (blackbox+whitebox tests RUN before close), so every primitive stays transitively used (no unused-warnings) and the deck validates end-to-end after each unit. Provenance (honest): THREE single-window builds have compiled `moon check`-clean yet overran before green tests → all reverted, behavior UNVERIFIED (so EACH session must RUN its tests, not stop at compile; string-scan call-forms verified vs core/async mbti, see memory). Builds 1-2 = a coarser M2.1 (~700 LOC — even "frame-only" bundled ALL shared machinery) → drove the M2.1a/b/c split. Build 3 (this session) = the M2.1b MONOLITH: the full HEAVY machinery compiled 0-warn (~500 LOC — so M2.1b's signatures below are a strong monolith-typechecked SEED, but NOT tree-reproducible: each split unit must re-prove 0-warn on landing, and the banked syntax has transcription gaps — e.g. package-private structs need the `priv` keyword, a bare `struct` warns `missing_priv`) but overran during an unforeseen TEST-RECONCILIATION (the new count/slide checks fail every empty-`<section>` M2.1a test → new builders + ~5 test fixes) → drove the M2.1b-i/b-ii split below. Signatures = TRANSCRIBE-and-adapt, not design-from-scratch: M2.1b-i/b-ii's were monolith-typechecked (build 3 — a strong seed, re-prove 0-warn on landing), M2.1c's remain unbuilt SEEDS; the staged contracts differ per slice (M2.1a has no slide loop, M2.1b-i rewires `no_slides`, M2.1c extends `scan_deck`'s consumers) so each compile-verifies only as it lands, and the validation LOGIC stays unproven until its tests RUN. 0-WARN INVARIANT (verified this review — see memory): a private helper unused in its OWN slice warns (`unused_value`); whitebox-test-only use does NOT rescue it, only `pub` or a real production caller does → every listed primitive needs an in-slice production caller, else it moves to its first-use slice or lands `pub`; likewise every package-private STRUCT needs the `priv` keyword (a BARE `struct` warns `missing_priv`) AND each field read in its slice (else `unused_field`). No stdlib HTML parser → targeted string-scan in CODEPOINT space (`Array[Char]`; memory's string-scanning note = the call-forms). Invariants ALL slices honor: CLEAN the doc ONCE up front = COMMENT-STRIP `<!--…-->` THEN RAW-TEXT-STRIP (blank `<style>`/`<script>` content, keep tags) → ALL scans (incl. M2.1b-i `scan_deck`) run on the cleaned doc (the template embeds `<section class="slide">` + `<img src="assets/…">` INSIDE comments AND a `<img src=…>` inside a CSS comment in `<style>` line ~83 → all must vanish, else false `frame.no_slides`/`img.not_asset`/`img.missing`; AS-BUILT M2.1a: `strip_raw_text(strip_comments(html.to_array()))`, unterminated raw-text BLANKED-TO-EOF (browser RAWTEXT-to-EOF) so an unclosed `<style>` cascades frame.style+body+no_slides; raw-text close matched at a name boundary); class-match = a whitespace-delimited TOKEN in a `class=` attr (single/double-quoted; tolerates multi-class `col-fig tall`), NOT exact-attr. Structured `Report` MUST(block)/SHOULD(warn); M2.1 findings all MUST. `templates/deck.html` = the structure contract (narrow-range Reads — it compresses on a full Read; its `.pageno` separator = U+00B7 with single spaces, verified; its only `<img>` + a `<section class="slide">` live INSIDE comments → strip removes them).
Types (M2.1a introduces, `pub(all)`): `Severity{Must;Should} derive(Eq)` · `Finding{severity:Severity,code:String,detail:String}` · `Report{findings:Array[Finding]}`; `must(code,detail)->Finding` (priv; a `should()` ctor DEFERS to M2.2 at first Should use — a private unused `should()` in M2.1a would `unused_value`-warn; the `Should` variant itself is safe now, an unconstructed `pub(all)` variant does NOT warn, verified) · `Report::ok(self)->Bool` (no Must) · `Report::codes(self)->Array[String]` · `Report::render(self)->String` (severity-labeled `"[MUST] code: detail"` / `"[SHOULD] …"` per line; `"ok"` if empty).
Hardening backlog (validate.mbt quote-safety; non-blocking, UNSCHEDULED — NOT a unit): the pre-cleaners `strip_comments`/`strip_raw_text` run BEFORE discovery and stay quote-UNAWARE → a `<!--`/`<style>`-shaped substring inside a quoted attr value (`data-note="<!--"`, `data-x="<style>"`) can mis-trigger stripping and BLANK real markup = fail-open. Discovery itself (`find_all_open_tags`/`find_by_class`) is now quote-safe, so this pre-clean pass is the SOLE residual. Fix = ONE unified quote+context-aware tokenizer walk (comment + raw-text + tag boundaries in a single pass) replacing both pre-cleaners. Deferred: near-zero risk for machine-generated (semi-trusted codex) decks, and a naive quote-aware `strip_comments` would corrupt `<script>if(a<b)…` (it runs before raw-text blanking, so JS/CSS `<letter` is not yet neutralized). Revisit at M2 review or if deck inputs become less trusted.
- **M2.1a Deck ENVELOPE validator** [DONE] — Report types + LIGHT scan primitives + document-envelope + image checks + the public entry points (NO slide loop yet). This is the HEAVIEST slice (the light primitives front-load here, ~450 LOC + tests) — banked signatures make it TRANSCRIPTION, but if the window runs hot, SPLIT before implementing: land the LIGHT primitives as a `pub` toolkit slice (M2.1a-i, blackbox-tested — `pub` dodges the unused warning; whitebox-only use does NOT, verified) then frame+img+public-api+slide-count (M2.1a-ii); `moon check` 0-warn = the split's acceptance gate. LIGHT primitives (no depth-match, no entity-decode, no class-scan discovery): `lower_ascii/is_ascii_letter/is_ascii_digit/is_attr_name_char(Char)` · `range_to_string(Array[Char],lo,hi)->String` · `index_ci(doc,needle:Array[Char])->Int` (needle lowercase; -1 absent) · `contains_ci` · `ci_eq(String,String)->Bool` · `skip_tag(doc,i,end)->Int` (quote-aware, past `>`) · `scan_open_tag(doc,lt)->(tag,attrs_lo,attrs_hi,past_gt)` (tag lowercased) · `get_attr(doc,lo,hi,name)->String?` (boolean attr→`Some("")`; ci name; single/double/unquoted values) · `has_class(doc,lo,hi,token)->Bool`+`has_token(value,token)->Bool`+`seg_eq` · `find_all_open_tags(doc,name)->Array[Int]` (`<` indices) · `strip_comments(Array[Char])->Array[Char]` (once; unterminated → swallow rest). Checks→codes: `check_frame(doc,out)` → `frame.doctype` (`<!doctype html` present, ci — `contains_ci`) · `frame.html_lang` (first `<html>`'s `lang`==`ja`, ci) · `frame.charset` (some `<meta charset>`==`utf-8`, ci) · `frame.style` (`style_ok`: `<style>` present ∧ `</style>` before `<body>` — unclosed `<style>` swallows the body → blank render) · `frame.body` (`body_ok`: `<body>`∧`</body>`∧`</html>` present); slide count = sections carrying class `slide` (`find_all_open_tags`+`has_class`) ≥1 → `frame.no_slides`; `check_imgs(doc,assets,out)` → every `<img src>` under `assets/` (`img.not_asset`) ∧ ∈ `assets` set (`img.missing`). Public: `pub validate_deck(html,slug,assets:@set.Set[String])->Report` (body = strip_comments → check_frame → no_slides → check_imgs; the per-slide loop is M2.1b — `slug` is reserved for M2.1b's pageno check, so if it unused-warns, `let _ = slug` + a forward-note) · `pub async list_assets(dir)->@set.Set[String]` (`dir/assets/*` → `assets/<name>`; ABSENT `dir/assets/` → EMPTY set via `@fs.exists`, NOT an error — a deck may ship zero figures) · `pub async validate_deck_dir(dir,slug)->Report` (read `dir/deck.html`+`list_assets` → `validate_deck`; raises if `deck.html` unreadable). Tests (`_test.mbt` blackbox + `_wbtest.mbt` whitebox): a builder emits a GOOD envelope (doctype+`<html lang="ja">`+`<meta charset="utf-8">`+closed `<style>`+`<body>…</body></html>`+≥1 empty `<section class="slide">`) → clean; a decoy `<section class="slide">`+`<img>` INSIDE a comment → still clean; seed each defect (drop doctype→`frame.doctype`; `lang="en"`→`frame.html_lang`; drop charset→`frame.charset`; unclosed `<style>`→`frame.style`; drop `</html>`→`frame.body`; zero sections→`frame.no_slides`; `<img src="foo.png">`→`img.not_asset`; `<img src="assets/x.png">`+empty assets→`img.missing`; same +`x.png`∈set→clean) → its code via `json_inspect(codes, content=[code])`; whitebox: `get_attr` (double/single/unquoted/boolean), `has_class` (multi-class `col-fig tall`→`tall` not `col`), `strip_comments` (drops comment; unterminated swallows), `index_ci` (ci hit; absent→-1); async: `validate_deck_dir` over a tmpdir bundle WITH + WITHOUT `assets/`; async: read shipped `templates/deck.html` located via `find_repo_root()` (reuse state.mbt — so it RUNS under `moon test`, not silently skips on a cwd change; skip only if the repo root truly can't be found) + assert its envelope+img checks clean (`slug="project"`). Accept: good envelope clean; each seeded envelope/img defect → its code; shipped template clean; gates green; no codex. AS-BUILT (single window, no split needed): all planned LIGHT primitives landed + `strip_raw_text`/`raw_text_open` ADDED (raw-text elem stripping, see invariant above); `count_slides` (M2.1b-i unifies to `scan_deck().length()`); optional-arg + pub-silences-unused mechanics → memory. 25 tests (was 14), 0-warn. ctx: 81% 162K/200K. CODEX-REVIEW follow-up: frame.style/body → tag/attr-aware + ORDERED (`find_all_open_tags`+`find_close_tag`, fixing a false-CLEAN on an unclosed style rescued by a stray earlier `</style>` + a false-fire on attributed `<body>`/`<style>`); unterminated raw-text BLANKED-TO-EOF (was "left intact", which leaked a decoy `<img>` as `img.not_asset`); doctype name-boundary; `list_assets` skips subdirs (`@fs.kind`); `contains_ci` dropped (unused); 28 tests, 0-warn
- **M2.1b-i SLIDE structure + COUNT checks** [DONE] — extend `validate.mbt` REUSING M2.1a primitives; add HEAVY DISCOVERY machinery + the slide MODEL (count fields) + COUNT checks; replace `count_slides` with `scan_deck` (`no_slides` = `scan_deck(doc).length()==0`; `validate_deck` signature UNCHANGED). LIGHT on checks BY DESIGN — this slice absorbs the whole TEST-RECONCILIATION (below), which is what overran the monolith. Machinery (monolith-typechecked this session — transcribe as a seed, re-prove 0-warn): `is_void_tag(String)->Bool` (area base br col embed hr img input link meta param source track wbr; NEW, first caller = `match_close`) · `is_close_tag_at(doc,i,name:Array[Char],end)->Int?` (name boundary: `</div>`≠`</division>`) · `match_close(doc,name,from,end)->(close_lt,past_gt)` (DEPTH-matched — nested same-name opens raise depth so a child's close ≠ the parent's; void→`(from,from)`; unbalanced→`(end,end)`) · `priv struct Elem { tag, token, lo, hi }` (`lo..hi` = inner-HTML span) · `match_class(doc,lo,hi,tokens)->String?` (loops `has_class`) · `find_by_class(doc,start,end,tokens)->Array[Elem]` (OUTERMOST matches, doc order; descends non-matches, SKIPS a match's body via `match_close` so siblings≠nested dups; resumes past a tag's real `>` → quote-safe, closing the M2.1a `<`-in-attr codex-defer for free). Model (all `priv struct` — a BARE `struct` warns `missing_priv`, verified): `priv struct Entry {}` (EMPTY marker; `raw`/`tiers` are M2.1c — a `priv` empty struct + `Entry::{}` are legal + warn-free) · `priv struct Slide { eyebrow_count:Int, pageno_count:Int, entries:Array[Entry] }` (EVERY field read by a check below, else `unused_field`) · `scan_deck(doc)->Array[Slide]` = `find_by_class(["slide"])` kept where `tag=="section"`; per slide `find_by_class(["eyebrow","pageno","entry"])` in its span, tally by `.token`, one `Entry{}` per `.entry`. Checks (`check_slides(slides,out)` — NO slug yet; keep M2.1a's `let _=slug`): one `.eyebrow`→`slide.eyebrow_count` · one `.pageno`→`slide.pageno_count` · ≥1 `.entry`→`slide.no_entry`. TEST-RECONCILIATION (do FIRST — count checks fail any non-swallowed empty `<section class="slide">`, breaking M2.1a's empty-section tests): add these proven blackbox builders — `fn entry()->String { "<div class=\"entry\"><div class=\"jp\">a</div><div class=\"jpf\">a</div><div class=\"en\">A</div></div>" }` · `fn eyebrow(n:String)->String { "<div class=\"eyebrow\"><span class=\"n\">\{n}</span><span>l</span></div>" }` · `fn pageno(text:String)->String { "<div class=\"pageno\">\{text}</div>" }` · `fn slide_of(e:String,es:String,p:String)->String { "<section class=\"slide\">\{e}\{es}\{p}</section>" }` — then set `envelope()`'s default `sections?` to `slide_of(eyebrow("01"), entry(), pageno("project \u{b7} 1 / 1"))` (correct text so M2.1b-ii inherits a passing default). RULE: any M2.1a test whose deck must stay CLEAN switches its section to a builder slide (the commented-decoy test's REAL section; the `frame.style`-ordering test's section; the two image-check tests — e.g. `slide_of(eyebrow("01"), entry()+"<img src=\"foo.png\">", pageno("project \u{b7} 1 / 1"))`; `validate_deck_dir`'s bundle). INTENTIONAL empties STAY: `sections=""`→`frame.no_slides`; unclosed-`<style>` cases (RAWTEXT-to-EOF swallows the section)→frame.style+body+no_slides. NEW count-defect blackbox tests: dup `.eyebrow`→`["slide.eyebrow_count"]` · dup `.pageno`→`["slide.pageno_count"]` · drop `.entry` (`slide_of(eyebrow("01"),"",pageno("project \u{b7} 1 / 1"))`)→`["slide.no_entry"]`. Whitebox (`_wbtest.mbt`): `scan_deck` on a builder deck → 1 slide, eyebrow_count=1/pageno_count=1/entries.length()=1 · `match_close` nested same-tag depth (`<div><div></div></div>` → the OUTER close) · `find_by_class` yields SIBLINGS not nested dups. Accept: good deck's structure clean; each seeded count defect→its code; M2.1a tests still green; `just check`/`test` 0-warn; no codex. AS-BUILT (single window, no split): the monolith seed transcribed cleanly — machinery + model + `check_slides` landed 0-warn, `count_slides` removed, `validate_deck` rewired to `scan_deck`+`check_slides`; the `<`-in-attr M2.1a defer is CLOSED for scan_deck's slide discovery (find_by_class quote-safe); at landing it still PERSISTED in check_frame/check_imgs (quote-unaware find_all_open_tags) — CLOSED in the codex-review follow-up below. Optional-arg default calling `slide_of(...)` VERIFIED. 32 tests (was 28: +1 count-defect blackbox, +3 whitebox scan_deck/match_close/find_by_class). ctx: 64% 127K/200K. CODEX-REVIEW follow-up: `find_all_open_tags` rewritten quote-AWARE (tag-by-tag walk via `skip_tag`, not a per-index `<name` scan) → fixes a frame FAIL-OPEN — an attr-embedded `<style></style>`/`<meta charset=utf-8>`/`<html lang=ja>`/`<body>` literal could FALSELY satisfy `frame.*` with no real element (the b-i "fail-closed" claim was WRONG: it was fail-OPEN); `find_by_class` gained `want_tag?` + `scan_deck` now DESCENDS a non-section `.slide` wrapper to check a nested `<section class="slide">` (was filtered out UNCHECKED = fail-open); `Elem` dropped its now-unread `tag` field; stale doc/test comments fixed (`validate_deck` doc → "envelope, slide-structure, and image contract"; "empty slide passes"→"complete slide"). Residual (TRACKED → Hardening backlog above): pre-cleaners `strip_comments`/`strip_raw_text` stay quote-UNAWARE (post-cleaning discovery is now fully quote-safe). 36 tests (was 32: +2 blackbox, +2 whitebox), 0-warn.
- **M2.1b-ii SLIDE TEXT checks + entity-decode** [DONE] — extend M2.1b-i; add entity-decode + `inner_text` + `two_digit` + the two TEXT checks (monolith-typechecked this session — seed). Machinery: `two_digit(n)->String` (`if n<10 {"0\{n}"} else {"\{n}"}`) · `trim_ws(String)->String` (trim ASCII ws via `is_space`; NEW, first caller = `inner_text`) · entity-decode {`named_entity(String)->Char?` (amp lt gt quot apos nbsp middot) · `hex_val(Char,base)->Int?` · `parse_entity(cs,i,n)->(Char,Int)?` (`&name;`/`&#dec;`/`&#xhex;`; reject codepoint>0x10FFFF) · `decode_entities(String)->String` (TOTAL — a bad `&` stays literal)} · `inner_text(doc,lo,hi)->String` (drop tags via `skip_tag`, then `trim_ws(decode_entities(range))`). EXTEND `Slide` with `eyebrow_n:String?`+`pageno_text:String?` (both READ by the checks below, else `unused_field`); EXTEND `scan_deck` to fill them (the eyebrow's `.n` via `find_by_class(["n"])`→`inner_text`; the `.pageno` Elem→`inner_text`). Thread `slug`: `check_slides(slides,slug,out)`, drop the `let _=slug`. Checks (1-based `idx`; `total`=slide count): `.n` inner-text==`two_digit(idx)`→`slide.eyebrow_n` (`match eyebrow_n { Some(t)=>t==want; None=>false }`) · `.pageno` inner-text==`"\{slug} \u{b7} \{idx} / \{total}"` (U+00B7; `inner_text` already DECODED entities so an escaped slug compares decoded)→`slide.pageno_format`. Tests: envelope default already passes (b-i built it correct) → ADD good 2-slide deck (`envelope(sections=s1+s2)`, slug `proj`; s1 `.n`=01 pageno `proj \u{b7} 1 / 2`, s2 `.n`=02 pageno `proj \u{b7} 2 / 2`)→`[]` · entity slug (`slug="A&B"`, pageno text `A&amp;B \u{b7} 1 / 1`)→`[]` · wrong `.n`→`["slide.eyebrow_n"]` · bad pageno→`["slide.pageno_format"]`. Whitebox: `decode_entities` (named + `&#dec;` + `&#xhex;` + unknown-stays-literal + missing-semicolon) · `inner_text` (tag-drop + decode + trim) · `scan_deck` fills correct `eyebrow_n`+`pageno_text`. EXTEND M2.1a's shipped-template test to assert full frame+slide checks clean on `templates/deck.html`. Accept: good deck (frame+slides) clean; each seeded slide defect→its code; shipped template clean; gates 0-warn; no codex. AS-BUILT (single window): monolith seed transcribed 0-warn. Text machinery (`two_digit`/`trim_ws`/entity-decode {`named_entity`/`hex_val`/`parse_entity`/`decode_entities`}/`inner_text`) + `Slide.{eyebrow_n,pageno_text}` + `scan_deck` first-fill (guard `if eyebrow_count==1`/`pageno_count==1`, avoids `is None`) + `check_slides(slides,slug,out)` (slug threaded, `let _=slug` dropped) with eyebrow_n/pageno_format checks. dup_pageno test reconciled (first pageno valid text → only slide.pageno_count; cross-slice: a text check on a count-checked element fires on the dup unless the first member is valid — dup_eyebrow's first `.n`=`01` already fine). Shipped-template test now asserts FULL frame+slide clean (slug=`project`; template `.n`=`01`/`02`, pageno=`project · N / 2` hexdump-verified) + negative-control proved it RUNS not skips. 41 tests (was 36: +3 blackbox — multi-slide-clean/decoded-slug/text-defects; +2 whitebox — decode_entities/inner_text; scan_deck wbtest extended, dup_pageno reconciled). ctx: 77% 154K/200K. CODEX-REVIEW follow-up: closed a dup-`.n` false-neg (`scan_deck` captures `eyebrow_n` only when the sole eyebrow holds exactly one `.n` — `ns.length()==1`); GATED each text check in the `else` of its count check so a missing/duplicated `.eyebrow`/`.pageno` fires ONLY its count code (was double-reporting one root cause); documented counts are over OUTERMOST layout slots (a `.pageno` nested inside another slot is uncounted BY DESIGN — a deep re-scan would regress find_by_class's sibling-vs-nested model); LOCKED entity totality/boundary via wbtests (surrogate `&#xD800;`/over-max `&#x110000;` stay literal, `&#x10FFFF;` decodes, leading-zeros no-overflow, `&AMP;` case-sensitive-literal, digitless `&#;`/`&#x;` literal, + a malformed-refs-are-identity property loop); tightened the `named_entity` doc (deck subset, not full HTML5). 45 tests (was 41: +2 blackbox dup-`.n`/missing-slot-gating, +2 whitebox boundary/totality); moon fmt/check/build/test 0-warn.
- **M2.1c Deck ENTRY + furigana validator** [DONE] — extend `validate.mbt` REUSING M2.1a/b primitives; `Entry` is EMPTY after M2.1b → M2.1c ADDS the tier model (`priv struct Tier { cls:String, html:String }`; `Entry` gains `raw:String`+`tiers:Array[Tier]`, BOTH read here else `unused_field`) and EXTENDS `scan_deck` to fill them (per `.entry`: raw inner-HTML + the `.jp`/`.jpf`/`.en` tier spans); wire per-`.entry` checks into the slide loop. Checks → codes: one `.jp`/`.jpf`/`.en` in DOM order → `entry.tiers`; JA TIER-CONSISTENCY `normalize(.jp)==normalize(.jpf)`, where `normalize` = strip tags keeping text nodes + DROP `<rt>` readings + DECODE entities + collapse/trim whitespace → `entry.ja_pairing` (cross-tier furigana guard: catches a kanji dropped/substituted BETWEEN the two JA tiers — NOT a source-truth check; a wrong-but-consistent reading passes → analyzer M2.5 + human); ruby well-formed → `ruby.rt_not_kana` (each `<rt>` all-kana) · `ruby.rt_count` (one `<rt>` per `<ruby>`) · `ruby.rt_outside_jpf` (ruby/rt live ONLY in `.jpf` — scan `Entry.raw` MINUS its `.jpf` tier(s): any `<ruby>`/`<rt>` elsewhere in the entry, not just `.jp`/`.en`, flags; per-tier html alone misses ruby outside all tiers) · `ruby.kanji_uncovered` (WITHIN `.jpf` ONLY — `.jp`/`.en` carry bare kanji by design → exempt). Coverage is PER-KANJI-CHARACTER via a single-pass state machine (in_ruby/in_rt/in_code): every `.jpf` kanji sits in SOME ruby base — NOT one-ruby-per-run (the template splits 要点一 into 要点+一 adjacent groups → a run-based check false-fails); `<code>` spans exempt; kanji/kana by Unicode range. Tests: good entries pass; each seeded ENTRY defect (unpaired jp/jpf · non-kana rt · uncovered kanji · malformed ruby · furigana-outside-jpf · missing tier) → its code; EXTEND the shipped-template test to assert the FULL validator (frame+slide+entry+furigana) is clean on `templates/deck.html` (`slug="project"`, empty assets) — its real hand-authored ruby is the key positive contract. Accept: good deck clean; each seeded ENTRY defect → its code; shipped template full-clean; gates green; no codex. AS-BUILT (single window): tier model (`Tier{cls,html}` + `Entry{raw,tiers}`) + `scan_deck` fills each `.entry`'s raw inner-HTML + `.jp`/`.jpf`/`.en` tier spans (`find_by_class`); NEW `classify_tag(doc,i,end)->(name_lc,is_close,past_gt)` (one-shot tag classify, reused by every furigana scan); furigana helpers `is_kanji`/`is_kana`/`all_kana`/`collapse_ws`/`normalize_ja`/`check_jpf_ruby`/`check_entry`; `check_entry` wired into the `check_slides` loop. `normalize_ja` = strip tags + DROP `<rt>` content + `decode_entities` + `collapse_ws`. `check_jpf_ruby` = single-pass state machine (in_ruby/in_rt/in_code) → `ruby.rt_not_kana` (each `<rt>` all-kana) · `ruby.rt_count` (one `<rt>` per `<ruby>`) · `ruby.kanji_uncovered` (per-kanji-CHARACTER: bare kanji outside ruby-base + outside `<code>`). `entry.tiers` GATES (one root cause → `return`, like the count checks); `entry.ja_pairing` = `normalize_ja(jp)!=normalize_ja(jpf)`; `ruby.rt_outside_jpf` = ruby/rt open-tag COUNT in `Entry.raw` minus in `.jpf` (`find_all_open_tags`) > 0. SELF-REVIEW (in-unit, not codex): an unclosed `<ruby>` never reaches its close-tag `rt_count` check mid-scan → added an end-of-scan `if in_ruby { bad_rt_count = true }` guard + `in_rt` reset in the ruby-close arm + a whitebox test. De-risked the positive contract in Python first (all 6 template entries pass; each seeded defect isolates to one code) BEFORE writing MoonBit. 52 tests (was 45: +2 blackbox — well-formed-entry incl. split-run 要点 + `<code>`-exemption, 6-defect mapping; +5 whitebox — normalize_ja, is_kanji/is_kana ranges, per-kanji coverage + code exemption, unclosed-ruby guard, scan_deck tier-fill). ctx: 86% 171K/200K. CODEX-REVIEW follow-up: HARDENED the furigana state machine against malformed input the happy-path missed — (F1) an unclosed `<rt>` inside a CLOSED `<ruby>` (its reading went kana-UNCHECKED) now flags `ruby.rt_count` (`|| in_rt` in the ruby-close arm + `in_rt` in the EOF guard); (F2) an unclosed `<code>` no longer grants a LASTING coverage exemption (track `code_hid_kanji`, EOF `if in_code && code_hid_kanji → uncovered`); (F3) `classify_tag` now BLANKS a name a non-boundary char cuts short, so `<rt2>`/`<ruby-foo>` can't masquerade as `<rt>`/`<ruby>` (same boundary rule as `find_all_open_tags`); (F4) a stray `<rt>` outside `<ruby>` → `rt_count`; (F5) an empty/ws-only `<rt>` → `rt_not_kana` (new `has_kana` — `all_kana("")` is vacuously true); (F6) a NESTED `<ruby>` → `rt_count` (flat-ruby convention). REJECTED (reasoned): F7 is_kanji iteration marks (々〆〇) would FALSE-FIRE a legit `<ruby>人<rt>ひと</rt></ruby>々` (々 shares the preceding kanji's reading; template has none), ー/・ stay valid kana (readings use them), Ext G/H YAGNI; F8 markup-in-`<rt>` can't fire on plain-kana template readings (contract); F9 descendant (non-direct-child) tiers are INTENTIONAL (same wrapper-descent as nested `.slide`). 59 tests (was 52: +7 whitebox — classify_tag boundary, fake-ruby, unclosed-rt, stray-rt, empty-rt, nested-ruby, unclosed-code); moon fmt/check/test 0-warn.
M2.2 = the provenance/gaps validator, THREE vertical slices by CHECK GROUP (per the M2.1b precedent + the impl-compiles-tests-overrun lesson — a 4th occurrence drove M2.2a's own split): SCHEMA + gaps + bundle shell (M2.2a-i) | SOURCE-ref + quote-grep (M2.2a-ii) | DECK cross-ref + coverage warnings (M2.2b). All live in a NEW `provenance.mbt` (+ `_test.mbt`/`_wbtest.mbt`) REUSING validate.mbt's package-private primitives (same package). SEED: the whole `provenance.mbt` (a-i+a-ii combined) compiled 0-warn this session before the test-write overran → the per-slice signatures below are TRANSCRIBE-and-adapt (re-prove 0-warn as each lands, per M2.1b's seed discipline; the a-i/a-ii boundary re-splits that monolith, so each slice's own 0-warn is unproven until it lands). Provenance CONTRACT (this unit DEFINES + enforces it): `provenance.json` = top-level JSON ARRAY of rows `{id,slide,claim,status:verified|inferred|gap,src:"path:START-END",quote:"<verbatim>"}` (1-based inclusive lines, decimal, no `L` prefix; `src` path = repo-parent-relative or absolute); deck claims carry `data-claim="id"`. Parse via `@json.parse`→`Json` enum HAND-WALK (`Json{Null;True;False;Number(Double,repr~:String?);String(String);Array;Object(Map[String,Json])}`, `map.get(k)->Json?`) — NOT derive(FromJson) (raises on mismatch, loses per-row granularity + specific `prov.schema` detail). Quote-grep = the quote LITERALLY occurs WITHIN the cited `src` line-SLICE (existence check, kills invented quotes; NOT truth — misattribution/cherry-pick still pass → M2.5 analyzer + human). Order in the combined Report: `validate_deck` (deck) → provenance rows (row order) → gaps. `src_base` (dir cited relative paths resolve against) = repo-parent in prod (M2.3 computes as `split_path(repo_root).0`), a tmpdir in tests; passed explicitly for testability. NO path normalization (`..`/`.`) — semi-trusted machine-generated citations, near-zero risk, note it (the real anti-injection guard is M2.3's post-run git-clean check). `should()` ctor + the `Should` severity DEFER to M2.2b's first SHOULD warning (M2.2a is all MUST).
  - **M2.2a-i Provenance SCHEMA + gaps + bundle shell** [OPEN] — the row-SCHEMA gate + gaps disclosure + async bundle shell; NO source-checking (a-ii). moon.pkg: add `moonbitlang/core/json` to the regular import block. NEW `provenance.mbt` REUSING validate.mbt (`validate_deck`/`list_assets`/`must`/`Report`/`trim_ws`/`ci_eq`) + `@json`. Types: `priv enum RowParse { Valid; Invalid(String) }` (NO payload yet — a-ii adds `Valid(ProvRow)`; both variants constructed so neither warns). `fn parse_row(row : Json) -> RowParse` HAND-WALKS the Json enum (accessors deprecated → pattern-match `Json::Object(m)`/`Json::String(s)`, `m.get(k) -> Json?`): non-Object → `Invalid("provenance row is not a JSON object")`; `id`:String else Invalid; `slide` present ∧ non-null (`match m.get("slide") { None | Some(Json::Null) => return Invalid(...); _ => () }`); `claim`:String; `status`:String ∈ {verified,inferred,gap}; if status ≠ "gap" then `src`+`quote` each present ∧ non-empty (folds 'no verified row lacks a quote'), gap may omit → all-OK ⇒ `Valid`. `fn check_gaps(text : String, out : Report) -> Unit`: any line (each `text.split("\n")` elem is a `StringView` → `.to_owned()` it before `trim_ws`, per state.mbt) whose `trim_ws` `ci_eq`s "no gaps" OR `has_prefix("- ")` ⇒ disclosed, else push `gaps.empty`. `fn check_provenance(prov_text : String, out : Report) -> Unit` (PLAIN fn — a-i does NO async I/O; a non-suspending `async fn` warns `unused_async` [0067], VERIFIED via probe — a-ii PROMOTES it to `async fn` once it adds the async `check_row` call): `@json.parse` in `try ... catch { _ => { out.findings.push(must("prov.parse", ...)); return } } noraise { j => j }`; `match j { Json::Array(a) => a; _ => { push prov.parse; return } }`; per row `match parse_row(row) { Invalid(msg) => push must("prov.schema", msg); Valid => () }`. `pub async fn validate_bundle_dir(bundle_dir : String, slug : String, src_base : String) -> Report` (`let _ = src_base` — only a-ii wires it; bundle_dir+slug ARE used): `validate_deck(read "\{bundle_dir}/deck.html" .text(), slug, list_assets(bundle_dir))` → if `@fs.exists(".../provenance.json")` `check_provenance(read .text(), out)` else push `prov.missing`; if `@fs.exists(".../gaps.md")` `check_gaps(read .text(), out)` else push `gaps.missing`; return out (raises only if deck.html unreadable). Codes (all MUST): prov.missing · prov.parse · prov.schema · gaps.missing · gaps.empty. Tests — whitebox `_wbtest.mbt`: a `pj(s) -> Json` helper (`try @json.parse(s) catch { _ => Json::Null } noraise { j => j }`) + a `row_result(Json) -> String` collapsing RowParse (`Valid` → "ok" / `Invalid(m)` → m) → parse_row valid-verified / gap-ok / verified-no-quote / not-object / bad-status / missing-id; a `gaps_codes(text) -> Array[String]` helper (`let r = Report::{ findings: [] }; check_gaps(text, r); r.codes()`) → check_gaps sentinel / bullet / prose-empty / case. Blackbox `_test.mbt` (async): COPY validate_test.mbt's existing `validate_deck_dir` tmpdir harness in SHAPE — `@async.with_task_group() <| group => { let root = @fs.tmpdir(prefix="slide-gen-prov"); group.add_defer(() => @fs.rmdir(root, recursive=true)); @fs.mkdir(bundle); @fs.write_file(path, content, create_mode=CreateOrTruncate); json_inspect(validate_bundle_dir(bundle, "project", root).codes(), content=[...]) }` — ASSERT INSIDE the closure, NO outer `mut` (validate_test.mbt:398-420 shows the exact shape; with_task_group returns its closure's value if ever needed) (do NOT grep mbti for with_task_group's return type — that derivation overran last session; the exact pattern already lives in validate_test.mbt). Reuse `envelope()`/`entry()` for a clean deck. Cases: clean bundle (valid `[...]` provenance.json + a `no gaps` gaps.md) → `[]`; no provenance.json → prov.missing; `{}` → prov.parse; `{` (malformed) → prov.parse; a bad-status row → prov.schema; no gaps.md → gaps.missing; prose-only gaps.md → gaps.empty. Accept: each seeded schema/gaps/shell defect → its code; clean bundle passes; `just` 0-warn; no codex.
  - **M2.2a-ii Source-ref + quote-grep** [OPEN] — extend M2.2a-i with the per-row anti-hallucination source check; introduces `ProvRow` + the pure src helpers; still all MUST. `priv struct ProvRow { id : String; status : String; src : String?; quote : String? }` (slide/claim validated-present in a-i but UNSTORED; EVERY field MUST be READ in-slice else `unused_field` — `id` via check_row's finding-detail strings (below), status/src/quote via its checks); change `RowParse::Valid` → `Valid(ProvRow)` and `parse_row`'s OK path now CAPTURES id/status/src/quote into it; UPDATE every `Valid` match arm to `Valid(_)` (a-i's `check_provenance` `Valid => ()` + the a-i wbtest `row_result` `Valid => "ok"`) or a-ii won't compile. Pure helpers (codepoint `Array[Char]` space): `fn parse_int(s) -> Int?` (positive decimal, None on empty/non-digit; reuses `is_ascii_digit`); `fn parse_src_ref(src) -> (String, Int, Int)?` (LAST `:` splits path↔range so a colon-bearing path survives; FIRST `-` splits the decimal range; `match (parse_int(a), parse_int(b)) { (Some(s), Some(e)) if s >= 1 && e >= s => Some((path, s, e)); _ => None }` — rejects non-positive / reversed / no-dash / no-colon); `fn line_slice(content, start, end) -> String?` (`content.split("\n").to_array()`; guard start ≥ 1 ∧ end ≥ start ∧ end ≤ len; join `[start-1..<end]` by `\n` via `StringBuilder` `write_stringview` per line — the split elems are `StringView` not `String`, so `write_stringview`, NOT `write_string`); `fn contains_substring(hay, needle) -> Bool` (literal substring; empty needle → true; nn > hn → false); `fn resolve_src(path, base) -> String` (`path.has_prefix("/")` ? path : `base + "/" + path`); `fn is_inside(path, dir) -> Bool` (`path == dir || path.has_prefix(dir + "/")`). `async fn check_row(r : ProvRow, bundle_dir : String, src_base : String, out : Report) -> Unit` (PROMOTE `check_provenance` to `async fn` HERE — it now makes the async check_row call; write EVERY `push` below as `must("<code>", "row \{r.id}: …")` so `r.id` is READ in-slice, else `unused_field: ProvRow.id` breaks 0-warn — this also makes findings row-identifiable): `guard r.status != "gap"` (gap → skip, unverifiable); unwrap r.src/r.quote (a-i guaranteed non-empty); `match parse_src_ref(src) { None => push prov.src_range; Some((path, start, end)) => { let resolved = resolve_src(path, src_base); if is_inside(resolved, bundle_dir) { push prov.self_citation; return }; if !@fs.exists(resolved) { push prov.src_missing; return }; let text = try @fs.read_file(resolved).text() catch { _ => "" } noraise { t => t }; match line_slice(text, start, end) { None => push prov.src_range; Some(slice) => if !contains_substring(slice, quote) { push prov.quote } } } }`. Wire: `check_provenance` gains `(bundle_dir, src_base)` params + its `Valid(r)` arm calls `check_row(r, bundle_dir, src_base, out)`; `validate_bundle_dir` drops `let _ = src_base`, threads `bundle_dir` + `src_base` through. Codes (all MUST): prov.src_range · prov.self_citation · prov.src_missing · prov.quote. Tests — whitebox: parse_src_ref via a `ref_str(s) -> String` collapsing the Option-tuple (valid `a.md:2-5` / colon-path `a:b.md:1-2` / reversed `x:5-2` / non-positive `x:0-1` / no-dash `x:5` / no-colon `x`); line_slice via `slice_str` (multi-hit / single / out-of-range / trailing-newline); contains_substring (hit / miss / empty / JA); resolve_src + is_inside (assert_true/false + inspect). Blackbox (REUSE a-i's harness, writing one extra source file under `root`): clean bundle citing `src.md:2-2` quote `beta` (src file `"alpha\nbeta gamma\ndelta\n"`) → `[]`; quote `zzz` → prov.quote; quote `delta` cited `src.md:2-2` (in file, off the cited line) → prov.quote (proves slice ≠ whole-file); relative `bundle/deck.html:1-1` (resolves inside bundle_dir) → prov.self_citation; `nope.md:1-1` → prov.src_missing; `src.md:50-60` → prov.src_range; a `gap`-status row (no src/quote) → `[]` (skips quote-grep). Accept: each seeded source defect → its code; the clean cited bundle passes; `just` 0-warn; no codex.
  - **M2.2b Deck cross-ref + coverage warnings** [OPEN] — extend `provenance.mbt`/`check_provenance` with the DECK-linkage slice; introduce `should()` (first `Should` use). Scan the CLEANED deck (`strip_raw_text(strip_comments(html))`) for all `data-claim` ids (walk tags via `scan_open_tag`+`get_attr`); every deck `data-claim` → a provenance row `id`, else `prov.dangling_claim` (MUST). SHOULD warnings (advisory review surface): each `.entry`/`.cap` (`find_by_class`) whose `inner_text(doc, elem.lo, elem.hi)` (REUSE the helper — a raw-span digit scan miscounts digits inside tags/attrs) carries a digit (ASCII `0-9` + fullwidth `０-９` U+FF10-19) with NO `data-claim` in its subtree → `prov.untagged_number`; every committed `assets/*.png` (from `list_assets`) whose deck `<img src>` lacks a `data-claim`∈row-ids → `prov.asset_uncited` (figure imgs must carry a claim citing figures.py+numbers — the contract M2.4's prompt targets). `validate_bundle_dir` threads the cleaned deck doc + assets + collected row-ids into the cross-ref. NOTE: the shipped `templates/deck.html` has NO `data-claim` yet (M2.4 introduces them) → M2.2's positive contract is a synthetic clean BUNDLE fixture, not the template. Tests: dangling-claim, untagged-number (tagged sibling stays clean), asset-uncited (a cited img passes), fully-tagged clean bundle PASS. Accept: each seeded cross-ref defect → its code; clean bundle passes; gates 0-warn; no codex.
- **M2.3 Codex harness + `generate <project>` + dry-run** [OPEN] — `generate.mbt`: pure `build_codex_argv` (bypass flags, prompt via stdin) + path resolution; codex authors a MULTI-FILE bundle → STAGE into a temp bundle dir (`decks/<project>.partial/` + `assets/`), validate the WHOLE staged dir, then promote (rename dir, replace existing); retry-once. `@process.collect_output` wrapped in the `timeout` binary; failure detection (exit≠0 · missing/empty `deck.html` · validator fail) leaves the stage un-promoted. Post-run write-confinement: verify codex touched ONLY the stage (sibling/source trees stay git-clean) — a dirtied source invalidates the run (closes quote-injection under bypass). Wire `generate <project>` (explicit target = any candidate, validated like `enable`; the no-arg enabled-set form is M2.6). `--dry-run` prints the exact argv + resolved paths + prompt, NO codex spend. `.gitignore`: untrack `decks/` (commit bundles); render-intermediate ignores stay narrow + are deferred to M3 (their on-disk path is an M3 decision) → verify any pattern with `git check-ignore` so committed `assets/*.png` stay tracked. First-draft prompt inline (real prompt = M2.4). Tests: argv / path / dry-run goldens; the harness's stage→validate→promote/retain proven with STUB bundles (hand-authored good/bad bundles dropped where codex would write — no prompt dependency); the live-codex path gated by an env flag (e.g. `SLIDE_GEN_LIVE`), mirroring M1.3's `SLIDE_GEN_BIN` e2e gate so bare `moon test` stays cheap. Accept: `--dry-run` prints the exact invocation; stub bundles drive promote-on-pass / retain-on-fail + retry; a gated live smoke run INVOKES codex and lands a bundle (validator PASS not required here — first passing bundle = M2.4).
- **M2.4 Prompt contract + live refinement** [OPEN, live-codex spend] — author `templates/deck-prompt.md` (loaded at runtime): embed `templates/deck.html` as the style contract; three-tier entries; INTRODUCE `data-claim` attrs into the template + prompt few-shot (the template lacks them today — this unit adds that forward contract); the `provenance.json`+`data-claim`+`gaps.md` contract with ONE verified-row + ONE gap-row few-shot; DRAFT→re-verify-quotes+line-numbers→FINAL self-critic; figure policy (author + run `figures.py` from committed cited numbers → `assets/*.png` + a citing provenance row; raw-data → placeholder+gaps); write the bundle to fixed paths, final message = a manifest; keep secrets/PHI out of the WHOLE bundle (content guideline — owner allows READING anything, but committed deck/provenance/gaps/assets must not dump creds/PHI). This unit lands the FIRST validator-PASSING live bundle. Refine against real codex runs on `spine-segmentation` (richest input + a hand-authored reference deck to compare) then `rehab` (committed aggregate numbers → a real figure) until the output passes the M2.1+M2.2 validator + emits an honest `gaps.md` (real gaps or the sentinel) — if refinement runs hot, close after the first project + carry the second to a follow-up unit. Accept: a live run yields a validator-passing bundle with honest gaps; the prompt is version-controlled + documented.
- **M2.5 Furigana analyzer cross-check** [OPEN] — project-local Python helper (`tools/furigana_check.py`, uv-managed; authoring-time only, never in the MoonBit binary): read the deck's `.jp`/`.jpf`, run fugashi+UniDic (or SudachiPy — decide at impl; UniDic ≈ best analyzer CER) to independently read the JA, compare vs codex's furigana; agreements ok, disagreements / analyzer-unknowns → append structured `gaps.md` entries (term · codex-reading · analyzer-reading · human-review). Wire post-codex in the generate pipeline. Tests: on a SMALL controlled JA fixture (common words the analyzer reads reliably) the helper flags a deliberately-wrong reading and passes a correct one; the spine reference deck is an INTEGRATION case where genuine analyzer disagreements (technical / proper-noun / loanword vocab) route to structured `gaps.md` entries, not test failures. Accept: flags a wrong reading + passes a correct one on the controlled fixture; spine disagreements surface as gaps; wired; authoring-time only.
- **M2.6 Batch over enabled set + robustness** [OPEN] — `generate` with no arg → every enabled project (from state); per-project failure isolation (one failure ≠ abort the batch); a summary report; retry-once; timeout / partial-output handling; idempotent regenerate (overwrite). Tests: batch dispatch over a fixture enabled-set (dry-run, no live codex); failure-isolation behavior. Accept: processes every enabled project, isolates failures, prints a summary; a gated live run over ≥2 enabled projects lands validated bundles.

Sequence: M2.1a→M2.1b-i→M2.1b-ii→M2.1c→M2.2a-i→M2.2a-ii→M2.2b (pure/async validators, no codex) → M2.3 (harness + dry-run + stub-bundle tests; live smoke env-gated) → M2.4 (prompt + FIRST passing live bundle) → M2.5 (furigana analyzer) → M2.6 (batch). Gates: extend the justfile (existing `fmt`/`check`/`build`/`test` + the Python tool's lint/test + the env-gated live-codex test).
