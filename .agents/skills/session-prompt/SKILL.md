---
name: session-prompt
description: Continue slide-gen from its roadmap in a fresh Codex session, or execute the explicit project task supplied with the invocation.
---

Read `.codex/prompts/session.md` completely and follow it as the workflow contract. Treat any explicit task supplied with this skill invocation as `TASK`; otherwise `TASK` is empty. Keep this adapter and the backing prompt synchronized whenever either changes.
