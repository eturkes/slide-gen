# Memory — slide-gen

Cross-session context + learned facts (agent-agnostic; read by Claude + Codex). Record only what git log / roadmap don't already hold.

## Project
- License `Apache-2.0 WITH LLVM-exception`. Per-source header when `.mbt`/scripts land: `SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception`.
- Stack + M1 plan + decision rationale live in `roadmap.md` — don't restate here.

## Prototype reference — `~/Projects/spine-segmentation/docs/slides/` (proven end-to-end; M2/M3 mirror it)
- `render.py`: runs figures.py → ONE tall chromiumfish screenshot → PIL slices into pages → PDF@72dpi (page pt == px). Counts pages by SOURCE-TEXT match — `DECK.read_text().count('<section class="slide">')`, NOT the DOM. VERIFIED this over-counts our template: `templates/deck.html` line-8 header comment holds the literal `<section class="slide">` → `grep -c` = 3 vs 2 real slides. → M3 MUST count via deck DOM (`querySelectorAll('section.slide').length`), not source match (the prototype's filled deck lacked the literal in its header, so its source-count sufficed; our generalized template reintroduced it).
- `deck.html`: hand-authored content in the template CSS (`<ruby>` furigana, three-tier JA/furigana/EN). → M2's value-add = automate that authoring via `codex exec`.
- `figures.py`: matplotlib figures from COMMITTED numbers; reads no `data/`, no PHI. `figure_example.py`: reads the Read-denied `data/` tree → a HUMAN runs it once, the PNG is committed (raw-data figures can't be agent-generated under deny rules).
- Assets may carry non-Apache licenses (e.g. CC BY-SA from source datasets) → per-asset caption attribution. Render intermediates (`page_*.png`, `_deck_full.png`) gitignored.
- Provenance: every number traces to project run-notes → M2 should cite sources + emit a gaps list (low-hallucination).

## MoonBit reference (native; pre-1.0 — verified at M1.1)
- Install: `curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash` → `~/.moon/bin` (moon, moonc, moon-lsp, moonrun…); NOT on non-interactive PATH → `export PATH="$HOME/.moon/bin:$PATH"` per Bash call, or drive via `just` (justfile prepends it). Pin via `MOONBIT_INSTALL_VERSION=<v>` or installer arg 1 (default `latest`); record exact via `moon version --all`. Native needs cc (build-essential ✓, x86_64).
- Config = DSL by default (flags rr_moon_mod/rr_moon_pkg on): `moon.mod` TOML-ish (`name="user/proj"` needs the slash; `preferred_target="native"`; module deps `import { "pkg@ver", }`); per-pkg `moon.pkg` (`import { "mod/pkg" @alias, }`; `supported_targets="+native"`; `options("is-main": true)`). Unused module dep = no warning. `moon fmt` blank-lines moon.mod fields + reflows calls. Core pkgs (argparse…) imported by path, bundled (no `moon add`).
- Layout: `moon new` → root lib pkg + `cmd/main` exe (NOT `src/main`); keep logic in lib, main thin. Output `_build/`; native ELF = `_build/native/{debug,release}/build/cmd/main/main.exe` (`.exe` on Linux). `.mooncakes/` = fetched deps (gitignored, readable for API ref via `examples/` + `*.mbti`). `moon install` deprecated → `moon build`/`check` auto-fetch.
- async (`moonbitlang/async@0.20.0`; native/LLVM Linux epoll): `async fn main` needs BOTH a `moonbitlang/async` package import (in the exe's `moon.pkg`, NOT just a source `@async`) AND a real suspension, else `unused_async` warns; `@fs` adds the `moonbitlang/async/fs` import → M1.1 sync; M1.2 flipped to `async fn main` (`moonbitlang/async` in cmd/main, `moonbitlang/async/fs` in root lib) at the first `@fs` await. async main may call raising fns directly (error boundary); sync `fn main` cannot → `try EXPR catch { e => … } noraise { v => … }` (`try?` deprecated). Concurrency = `with_task_group`. `@process.collect_output_merged → (Int, &@io.Data)`; `@fs.{read_file, write_file(p, content, create_mode=CreateOrTruncate), readdir(p, include_hidden?, include_special?, sort?), kind(p, follow_symlink?)→FileKind, tmpdir(prefix~), mkdir, symlink(linkpath, target~), rmdir(p, recursive=true)}`. `kind` on FULL `parent/name` (bare resolves vs cwd), FOLLOWS symlinks by DEFAULT (`follow_symlink=false`=lstat) + raises on unresolvable target → ONE follow-stat classifies dir / symlink→dir (candidate) vs broken / non-dir, so the planned lstat+stat 2-call split was redundant. write content: String IS `@io.Data` (`""`=empty file); `&@io.Data`≠String on READ → `.text()`/`.binary()`. `rmdir(recursive=true)` unlinks symlinks (no traverse). `defer` REJECTS async/raising calls → cleanup via `@async.with_task_group() <| group => { group.add_defer(() => <async cleanup>) }`. `@env.current_dir()→String?` (core/env) for cwd. `x/fs.is_dir` follows symlinks + no lstat → use `@fs.kind`.
- `@argparse` (clap-like, subcommands CONFIRMED): `@argparse.Command(name, about?, flags?, options?, positionals?, subcommands?, version?, arg_required_else_help?, subcommand_required?, …)` (`::new` deprecated → call `Command(...)`). `cmd.parse(argv? : ArrayView[String], env? : Map[String, String]) -> Matches raise` (test a dispatch key via `cli().parse(argv=["list"]).subcommand`; an `Array` literal coerces to the view) auto-prints+exits on `-h/--help` (and `--version` only when `version=` is set on the `Command`), raises on bad args (msg is `Show` → `println(err)`, currently stdout — M1.3 routes to stderr + nonzero exit); no-arg = real process args. `Matches.subcommand : (String, Matches)?`. `cmd.render_help() -> String` for goldens. `runtime_exit_native` not publicly callable → exit via libc FFI `extern "c" fn c_exit(code:Int) = "exit"` (argparse's own primitive), wrapped never-returning `async fn[T] die(msg,code)->T { @stdio.stderr.write(msg); c_exit(code); abort("unreachable") }` (bottom type → fits any match arm; needs `moonbitlang/async/stdio` in cmd/main).
- Tests: `inspect(v, content=…)` snapshot, fill/update via `moon test --update`; `json_inspect(arr, content=[...])` for arrays. Multiline `#|` only in `let`/parens, NOT as a bare `content=` arg (warns) → single-line goldens use plain quoted strings. `inspect` on a tuple warns (Show-for-debug deprecated) → inspect interpolated Strings (`"\{a}|\{b}"`) instead. `_wbtest.mbt` whitebox (sees private), `_test.mbt` blackbox. `async test` requires `moonbitlang/async` in the TEST's scope; `for "test"` import blocks feed BLACKBOX only, NOT whitebox → an async test in a lib whose non-test code doesn't use `@async` must be blackbox (`_test.mbt`) with `moonbitlang/async` in a `for "test"` block. Blackbox refs the ROOT pkg's pub items UNQUALIFIED (the hyphen in `eturkes/slide-gen` yields no valid auto-alias; `@lib` is cmd/main-private, invisible to blackbox). `@quickcheck` = Arbitrary + `samples` gen (no shrinker/fuzzer). `@json` derive ToJson/FromJson; no TOML lib → newline state file.
- `String::compare` / `<` / `>` = SHORTLEX (length first, THEN content), NOT lexicographic → `Array::sort` orders by length. Dictionary order needs `Array::sort_by(cmp)` with an explicit codepoint comparator (`s.to_array()` → compare `Char` by `<`/`>`, shared prefix → shorter first). VERIFIED `"link_to_dir" < "realdir"`=false but `"link" < "real"`=true. `list`/state-file sorting (M1.3) must use this comparator, not the default. Deprecations (0.10.x): `not(x)`→`!x`; `StringView::to_string`→`.to_owned()`; `String::substring(start?,end?)`→slice `s[a:b]` (returns StringView).
- M1.3 (state + exit + e2e): `pub enum` exposes the type but variants stay read-only externally (Error 4036 on construction) → `pub(all) enum` when main/blackbox build variants. `derive(Show)` DEPRECATED (→`Debug`/manual) → Eq-only enums, compare `==`/`!=` (not Show-interp); `inspect` on non-String (Array/tuple) warns Show-for-debug, String special-cased (no warn) → assert via `.length()`+`[i]` or interpolated String. Blackbox `_test.mbt` sees the lib's REGULAR import-block pkgs too (`@fs` `@set` `@env`), not only `for "test"`, AND their enum variants unqualified (`create_mode=CreateOrTruncate`). Property gen direct via `@splitmix.new(seed?=37)→RandomState` `.next_positive_int()` (splitmix = quickcheck's RNG; cleaner than a custom Arbitrary; dropped unused `core/quickcheck`, kept `.../splitmix`). `@process.collect_output(cmd:StringView, args:ArrayView[String], extra_env?, inherit_env?, stdin?, cwd?, …)→(Int, &@io.Data stdout, &@io.Data stderr)` (String→StringView, Array→ArrayView auto-coerce; `&@io.Data::text()→String raise`). `@env.get_env_var(String)→String?` (core/env). `moon test --update` fills a bare `inspect(strWithNewlines)` as `#|` multiline goldens in place. e2e gate: justfile `test: build` + `SLIDE_GEN_BIN="$(realpath "$(find _build/native -name main.exe -path '*cmd/main*' -print -quit)")" moon test` (absolute — e2e cwd=tmpdir); the test early-returns when unset so a bare `moon test` stays green. Atomic state write: FIXED temp `path+".tmp"` (NOT roadmap's unique-temp — single-user last-writer-wins, MoonBit lacks clean pid/rand; a crash leaves one stale temp the next write overwrites; gitignore `enabled.txt.tmp`) → `@fs.write_file(tmp, s, create_mode=CreateOrTruncate, sync=@fs.Full)` + `@fs.rename(tmp, path, replace=true)`. Repo-root anchor: walk up to a `moon.mod` whose content declares `name = "eturkes/slide-gen"` via a LINE-ANCHORED match (the `name` decl must begin a line — file start or post-newline; DSL format, NOT `moon.mod.json`; rejects both a foreign ancestor's `moon.mod` and the literal embedded in another field's value); `ascend` read-and-catches, treating absent/unreadable as non-marker; stop at fs root when `split_path(dir).0 == dir`. Codex-review (M1.3) confirmed/added: `Char::is_whitespace()` (builtin; full Unicode White_Space incl. NBSP/U+3000/U+2028-9) → `representable` edge check rejects any Unicode edge-space, not only ASCII 0x20; `@stdio.stderr.write` DOES flush before libc `c_exit` (verified — e2e ghost-case asserts the exact stderr golden) so the `die` pattern delivers its message; `representable` also rejects a leading `-` (argparse reads a `-name` positional as an option unless a `--` separator precedes it, so such a dir is excluded rather than listed-but-`enable`-able only via `enable -- -name`) and C1 controls (0x7f-0x9f, not just C0+DEL).
- Serena + tokensave: neither indexes MoonBit (Serena `moon-lsp` in `~/.moon/bin` unwired; tokensave reads `.mbt` as `Other` → empty graph, no symbols) → nav via grep + `moon check`/`mooninfo`; core APIs from `~/.moon/lib/core/<pkg>/pkg.generated.mbti` + READMEs.
- String scanning (M2.1 validator; no stdlib HTML parser) — call-forms below COMPILE (re-verified vs core/async mbti; behavior untested): scan in CODEPOINT space — `String::to_array()→Array[Char]`, index-walk, rebuild via `StringBuilder::new()` (an `#alias` of the `StringBuilder(size_hint?)` ctor) + `.write_char(Char)`/`.write_string(String)` (methods of the `Logger` trait it impls) / `.to_string()` — dodges UTF-16 surrogate hazards on JA. `Char::to_int()→Int` + Unicode-range tests classify kanji/kana; `Int::to_char()→Char?` (invalid codepoint = None); `String::has_prefix(prefix : StringView)→Bool` (a string literal coerces). `@set.Set(arr[:])` builds from an ArrayView (`#as_free_fn from_array`; `[][:]` = empty) + `.contains(x)→Bool`. ASCII case-fold = `+0x20` on codepoints A-Z (no alloc) → case-insensitive tag matching without lowering the whole string. DESIGN CHOICE (prior impl, untested): scan-in-`Array[Char]` + verbatim `StringBuilder` extraction over StringView slicing for interleaved structure/content work. ASYNC `@fs.exists(path)→Bool` + `@fs.remove(path)→Unit` (unlink a file) alongside the async `@fs.{…}` set above; `try @fs.kind(p) catch { _ => … } noraise { @fs.Directory => …, _ => … }` classifies dir-vs-file in one follow-stat.

## Env (verified)
- codex exec (0.142.4; model per `~/.codex/config.toml` = gpt-5.5 xhigh): `codex exec [PROMPT|stdin]` (stdin appended as `<stdin>` if both given). Flags: `-C/--cd` working root · `--add-dir` extra writable roots · `-s/--sandbox {read-only(DEFAULT),workspace-write,danger-full-access}` (exec DEFAULTS read-only → must raise to write) · `--dangerously-bypass-approvals-and-sandbox` (legit for externally-sandboxed hosts; exec has NO `-a/--ask-for-approval` — that's top-level `codex` only, exec REJECTS it exit 2, approval moot in non-interactive exec) · `--skip-git-repo-check` · `-o/--output-last-message` (final MSG only, NOT artifacts) · `--json` (JSONL; `turn.completed` vs `turn.failed`) · `--output-schema` (UNRELIABLE — silently dropped when tools/MCP active) · `-m/--model` · `-c key=value` (e.g. `-c model_reasoning_effort=low` PER-INVOCATION — config-level effort has a reset bug) · `--ephemeral`. TRUST: exec shows NO trust prompt; `[projects."path"].trust_level` gates only whether project-local `.codex/` LOADS, not runtime → explicit `--sandbox` runs ANY dir (VERIFIED M2 plan: read-only exec on a non-trusted sibling → exit 0, replied). RELIABILITY: success = exit 0 AND the artifact exists/non-empty/validates (exit 0 can still truncate → ALWAYS validate the file, not just the code); codex has no idempotency + writes a MULTI-FILE bundle → stage into a temp bundle DIR, validate the WHOLE dir, then promote (rename dir, replace existing); retry-once; wrap in external `timeout` (only per-provider retries exist). Bypass = NO write confinement → codex CAN write outside the bundle → post-run verify only the bundle changed (sibling/source trees stay git-clean; a dirtied source invalidates the run + closes quote-injection). COST: ~18K tokens for a trivial probe → real deck generation far heavier. → M2 has codex WRITE the bundle to fixed paths; final message = a manifest.
- furigana (JA kanji→kana reading) tooling (2026 research): frontier LLM inline ≈0.5% kana-CER (best overall) but silently emits fluent WRONG kana; deterministic analyzers UniDic≈1.5% / Sudachi≈1.8% CER, worse on technical/proper-noun/loanword vocab + emit one best path with no ambiguity signal. HYBRID (LLM draft + analyzer cross-read, disagreements→human review) = SOTA honest approach; no dedicated agent furigana tool exists. Cheap deterministic honesty guards (no analyzer, MoonBit-checkable): strip `<rt>` from `.jpf` → residue must == the `.jp` tier (CROSS-TIER consistency: catches a kanji dropped/substituted between tiers, NOT source-truth); `<rt>` all-kana; ruby coverage (WITHIN `.jpf`) is PER-KANJI-CHARACTER (every kanji sits in some ruby base, NOT one-ruby-per-run — a kanji run may split across adjacent ruby groups, e.g. 要点一 as 要点+一, which a run-based check false-fails). Analyzer verifier = fugashi+unidic-lite (~50MB) or SudachiPy+SudachiDict — authoring-time only, never in the binary.
- chromiumfish (headless Chromium via playwright-core) + Noto Sans CJK JP both present → render stack ready for M3. `img2pdf` installed via `uv tool` this session (alternative to PIL for the PDF step).
