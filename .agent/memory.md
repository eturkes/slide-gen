# Memory — slide-gen

Cross-session context + learned facts for Codex. Record only what git log / roadmap don't already hold.

## Project
- License `Apache-2.0 WITH LLVM-exception`. Per-source header when `.mbt`/scripts land: `SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception`.
- Stack + M1 plan + decision rationale live in `roadmap.md` — don't restate here.
- Unit-sizing (VERIFIED across M2.1/M2.2): a validator/harness unit spanning multiple check-groups rarely lands implementation + full tests cleanly in one work unit, even from a typechecked signature seed → split by concern into independently verified slices (M2.1b-i/b-ii, M2.2a-i/a-ii, M2.2b-i/b-ii). If implementation typechecks but the unit cannot reach green tests cleanly, bank only the confirmed design/facts/read pointers in the roadmap; resume from a fresh self-contained slice. Apply to the M2.3 harness + M3 render.

## Prototype reference — `~/Projects/spine-segmentation/docs/slides/` (proven end-to-end; M2/M3 mirror it)
- `render.py`: runs figures.py → ONE tall chromiumfish screenshot → PIL slices into pages → PDF@72dpi (page pt == px). Counts pages by SOURCE-TEXT match — `DECK.read_text().count('<section class="slide">')`, NOT the DOM. VERIFIED this over-counts our template: `templates/deck.html` line-8 header comment holds the literal `<section class="slide">` → `grep -c` = 3 vs 2 real slides. → M3 MUST count via deck DOM (`querySelectorAll('section.slide').length`), not source match (the prototype's filled deck lacked the literal in its header, so its source-count sufficed; our generalized template reintroduced it).
- `deck.html`: hand-authored content in the template CSS (`<ruby>` furigana, three-tier JA/furigana/EN). → M2's value-add = automate that authoring via `codex exec`.
- `figures.py`: matplotlib figures from COMMITTED numbers; reads no `data/`, no PHI. `figure_example.py`: reads the Read-denied `data/` tree → a HUMAN runs it once, the PNG is committed (raw-data figures can't be agent-generated under deny rules).
- Assets may carry non-Apache licenses (e.g. CC BY-SA from source datasets) → per-asset caption attribution. Render intermediates (`page_*.png`, `_deck_full.png`) gitignored.
- Provenance: every number traces to project run-notes → M2 should cite sources + emit a gaps list (low-hallucination).

## MoonBit reference (native; pre-1.0 — verified at M1.1)
- Install: `curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash` → `~/.moon/bin` (moon, moonc, moon-lsp, moonrun…); NOT on non-interactive PATH → `export PATH="$HOME/.moon/bin:$PATH"` per Bash call, or drive via `just` (justfile prepends it). Pin via `MOONBIT_INSTALL_VERSION=<v>` or installer arg 1 (default `latest`); record exact via `moon version --all`. Native needs cc (build-essential ✓, x86_64).
- Config = DSL by default (flags rr_moon_mod/rr_moon_pkg on): `moon.mod` TOML-ish (`name="user/proj"` needs the slash; `preferred_target="native"`; module deps `import { "pkg@ver", }`); per-pkg `moon.pkg` (`import { "mod/pkg" @alias, }`; `supported_targets="+native"`; `options("is-main": true)`). Unused module dep = no warning. Unused PRIVATE top-level fn = `unused_value` warning (verified M2 review); a private fn used ONLY from a `_wbtest.mbt` STILL warns (whitebox-test use ≠ used) — only `pub` or a real production caller silences it; an unconstructed `pub(all)` enum VARIANT does NOT warn; a package-private struct must be spelled `priv struct` — a BARE non-pub `struct X {}` warns `missing_priv` (0004: "does not occur in public signature"), whereas a `priv struct X {}` (empty or fielded) + literal `X::{}` are legal + warn-free, and its UNREAD field warns `unused_field` (0007); `pub(all)` silences both (0004 + 0007 re-verified on moon 0703 this review) → in a 0-warn package every private helper needs a production caller AND every struct is `priv` with every field read in its OWN slice, test-only helpers must be `pub`. `moon fmt` blank-lines moon.mod fields + reflows calls. Core pkgs (argparse…) imported by path, bundled (no `moon add`).
- Layout: `moon new` → root lib pkg + `cmd/main` exe (NOT `src/main`); keep logic in lib, main thin. Output `_build/`; native ELF = `_build/native/{debug,release}/build/cmd/main/main.exe` (`.exe` on Linux). `.mooncakes/` = fetched deps (gitignored, readable for API ref via `examples/` + `*.mbti`). `moon install` deprecated → `moon build`/`check` auto-fetch.
- async (`moonbitlang/async@0.20.0`; native/LLVM Linux epoll): `async fn main` needs BOTH a `moonbitlang/async` package import (in the exe's `moon.pkg`, NOT just a source `@async`) AND a real suspension, else `unused_async` warns; `@fs` adds the `moonbitlang/async/fs` import → M1.1 sync; M1.2 flipped to `async fn main` (`moonbitlang/async` in cmd/main, `moonbitlang/async/fs` in root lib) at the first `@fs` await. async main may call raising fns directly (error boundary), and ANY `async fn` implies raise-capability — no explicit `raise` keyword (an async validator fn calls raising `@fs`/`@json` directly, handled via try/catch/noraise); BUT `unused_async` [0067] fires on ANY async fn (not just main) with NO suspension point (VERIFIED this review by probe) → do NOT mark a fn async merely for raise-capability: a fn needing only raise-ABSORPTION stays a PLAIN fn (`try/catch/noraise` is TOTAL — neither `async` nor `raise` in its signature), becoming `async` only when it makes a real suspending call (`@fs` I/O). E.g. a provenance fn doing `@json.parse` (sync, raising) + pure walking = PLAIN fn; it turns `async` only once it calls `@fs`. sync `fn main` cannot → `try EXPR catch { e => … } noraise { v => … }` (`try?` deprecated). Concurrency = `with_task_group`. `@process.collect_output_merged → (Int, &@io.Data)`; `@fs.{read_file, write_file(p, content, create_mode=CreateOrTruncate), readdir(p, include_hidden?, include_special?, sort?), kind(p, follow_symlink?)→FileKind, realpath(p : StringView)→String (resolves symlinks + ./..), tmpdir(prefix~), mkdir, symlink(linkpath, target~), rmdir(p, recursive=true)}`. `kind` on FULL `parent/name` (bare resolves vs cwd), FOLLOWS symlinks by DEFAULT (`follow_symlink=false`=lstat) + raises on unresolvable target → ONE follow-stat classifies dir / symlink→dir (candidate) vs broken / non-dir, so the planned lstat+stat 2-call split was redundant. write content: String IS `@io.Data` (`""`=empty file); `&@io.Data`≠String on READ → `.text()`/`.binary()`. `rmdir(recursive=true)` unlinks CHILD symlink ENTRIES (no traverse) BUT opendirs + FOLLOWS a ROOT symlink-to-dir → deletes the TARGET's contents (guard the stage root with `kind(follow_symlink=false) is Directory` before recursive-rmdir — M2.3b-ii). `defer` REJECTS async/raising calls → cleanup via `@async.with_task_group() <| group => { group.add_defer(() => <async cleanup>) }`. `@env.current_dir()→String?` (core/env) for cwd. `x/fs.is_dir` follows symlinks + no lstat → use `@fs.kind`.
- `@argparse` (clap-like, subcommands CONFIRMED): `@argparse.Command(name, about?, flags?, options?, positionals?, subcommands?, version?, arg_required_else_help?, subcommand_required?, …)` (`::new` deprecated → call `Command(...)`). `cmd.parse(argv? : ArrayView[String], env? : Map[String, String]) -> Matches raise` (test a dispatch key via `cli().parse(argv=["list"]).subcommand`; an `Array` literal coerces to the view) auto-prints+exits on `-h/--help` (and `--version` only when `version=` is set on the `Command`), raises on bad args (msg is `Show` → `println(err)`, currently stdout — M1.3 routes to stderr + nonzero exit); no-arg = real process args. `Matches{subcommand:(String,Matches)?; flags:Map[String,Bool]; values:Map[String,Array[String]]}` → a subcommand FLAG via `sub.flags.get(id)->Bool?` (`Some(true)`=present; absent = None|Some(false) → test `is Some(true)`, robust to both), a POSITIONAL via `sub.values.get(name)`; declare a flag `@argparse.FlagArg(id, long?, short?, about?, action?, …)` inside `Command(flags=[…])` (M2.3a `generate --dry-run`). `cmd.render_help() -> String` for goldens — adding a subcommand/flag SHIFTS that golden → `moon test --update`. `runtime_exit_native` not publicly callable → exit via libc FFI `extern "c" fn c_exit(code:Int) = "exit"` (argparse's own primitive), wrapped never-returning `async fn[T] die(msg,code)->T { @stdio.stderr.write(msg); c_exit(code); abort("unreachable") }` (bottom type → fits any match arm; needs `moonbitlang/async/stdio` in cmd/main).
- Tests: `inspect(v, content=…)` snapshot, fill/update via `moon test --update`; `json_inspect(arr, content=[...])` for arrays. Multiline `#|` only in `let`/parens, NOT as a bare `content=` arg (warns) → single-line goldens use plain quoted strings; `$|` INTERPOLATED multiline (`\{expr}` per line; bare `{`/`|`/`"` stay literal, so JSON-shape + `class="…"` text needs no escaping) obeys the same rule — a fn whose body IS a `$|` block must PAREN-wrap it (`fn f()->String { ( $|… ) }`), VERIFIED 0-warn building the M2.3a prompt. `inspect` on a tuple warns (Show-for-debug deprecated) → inspect interpolated Strings (`"\{a}|\{b}"`) instead. `_wbtest.mbt` whitebox (sees private), `_test.mbt` blackbox. `async test` requires `moonbitlang/async` in the TEST's scope; `for "test"` import blocks feed BLACKBOX only, NOT whitebox; a `for "wbtest"` import block feeds whitebox (confirmed M2.3b-ii). Async higher-order params compile as `f~ : async (T) -> R`; local async fns capture mutable test state. A private production fn used only from wbtest still unused-warns → b-ii's temporary zero-warn seam uses `pub struct GenPlan` (opaque fields; `pub(all)` would expose them) + `pub async fn stage_validate_promote`; b-iii restored both to private once the real caller landed. Blackbox refs the ROOT pkg's pub items UNQUALIFIED (the hyphen in `eturkes/slide-gen` yields no valid auto-alias; `@lib` is cmd/main-private, invisible to blackbox). `@quickcheck` = Arbitrary + `samples` gen (no shrinker/fuzzer). `@json` derive ToJson/FromJson OR hand-walk the `Json` enum (accessors as_object/as_string DEPRECATED → pattern-match `Json::{Object(Map[String,Json])|String(String)|Array(Array[Json])|Number(Double,repr~:String?)|Null|True|False}`, `Map::get(k)->V?`, or-patterns like `None | Some(Json::Null)`); `@json.parse(StringView)->Json raise ParseError` (String coerces; `try … catch {_=>fallback} noraise {j=>j}` in a PLAIN non-async fn — @json.parse+walk needs no suspension); hand-walk BEATS derive(FromJson) for per-row/field error granularity (derive raises on first mismatch, loses which row/field). CONSTRUCTING a `Json` needs the public ctor fns (`Json::null()`/`Json::boolean(Bool)`/`Json::array(Array[Json])`) — the bare variants (`Json::Null`…) are read-only externally (Error 4036, `Json` is `pub` not `pub(all)`, like any non-`pub(all)` enum), though pattern-MATCHING them (incl. `guard j is Json::Object(m)`) is fine. no TOML lib → newline state file.
- `String::compare` / `<` / `>` = SHORTLEX (length first, THEN content), NOT lexicographic → `Array::sort` orders by length. Dictionary order needs `Array::sort_by(cmp)` with an explicit codepoint comparator (`s.to_array()` → compare `Char` by `<`/`>`, shared prefix → shorter first). VERIFIED `"link_to_dir" < "realdir"`=false but `"link" < "real"`=true. `list`/state-file sorting (M1.3) must use this comparator, not the default. Deprecations (0.10.x): `not(x)`→`!x`; `StringView::to_string`→`.to_owned()`; `String::substring(start?,end?)`→slice `s[a:b]` (returns StringView).
- M1.3 (state + exit + e2e): `pub enum` exposes the type but variants stay read-only externally (Error 4036 on construction) → `pub(all) enum` when main/blackbox build variants. `derive(Show)` DEPRECATED (→`Debug`/manual) → Eq-only enums, compare `==`/`!=` (not Show-interp); `inspect` on non-String (Array/tuple) warns Show-for-debug, String special-cased (no warn) → assert via `.length()`+`[i]` or interpolated String. Blackbox `_test.mbt` sees the lib's REGULAR import-block pkgs too (`@fs` `@set` `@env`), not only `for "test"`, AND their enum variants unqualified (`create_mode=CreateOrTruncate`); ALL `_test.mbt` of a package share ONE blackbox module → a helper `fn` in one file (validate_test.mbt's `envelope()`/`entry()`) is callable from another (provenance_test.mbt reuses them), and a test-file-local `fn`/`async fn` used only by tests in that module does NOT unused-warn (unlike a PRODUCTION private fn whose only caller is a wbtest). Property gen direct via `@splitmix.new(seed?=37)→RandomState` `.next_positive_int()` (splitmix = quickcheck's RNG; cleaner than a custom Arbitrary; dropped unused `core/quickcheck`, kept `.../splitmix`). `@process.collect_output(cmd:StringView, args:ArrayView[String], extra_env?, inherit_env?, stdin?, cwd?, …)→(Int, &@io.Data stdout, &@io.Data stderr)` (String→StringView, Array→ArrayView auto-coerce; `&@io.Data::text()→String raise`; `stdin` requires `&ProcessInput` — create via `@process.write_to_process()`, feed concurrently, close the writer for EOF; String does NOT implement ProcessInput). `@env.get_env_var(String)→String?` (core/env). `moon test --update` fills a bare `inspect(strWithNewlines)` as `#|` multiline goldens in place. e2e gate: justfile `test: build` + `SLIDE_GEN_BIN="$(realpath "$(find _build/native -name main.exe -path '*cmd/main*' -print -quit)")" moon test` (absolute — e2e cwd=tmpdir); the test early-returns when unset so a bare `moon test` stays green. Atomic state write: FIXED temp `path+".tmp"` (NOT roadmap's unique-temp — single-user last-writer-wins, MoonBit lacks clean pid/rand; a crash leaves one stale temp the next write overwrites; gitignore `enabled.txt.tmp`) → `@fs.write_file(tmp, s, create_mode=CreateOrTruncate, sync=@fs.Full)` + `@fs.rename(tmp, path, replace=true)`. Repo-root anchor: walk up to a `moon.mod` whose content declares `name = "eturkes/slide-gen"` via a LINE-ANCHORED match (the `name` decl must begin a line — file start or post-newline; DSL format, NOT `moon.mod.json`; rejects both a foreign ancestor's `moon.mod` and the literal embedded in another field's value); `ascend` read-and-catches, treating absent/unreadable as non-marker; stop at fs root when `split_path(dir).0 == dir`. Codex-review (M1.3) confirmed/added: `Char::is_whitespace()` (builtin; full Unicode White_Space incl. NBSP/U+3000/U+2028-9) → `representable` edge check rejects any Unicode edge-space, not only ASCII 0x20; `@stdio.stderr.write` DOES flush before libc `c_exit` (verified — e2e ghost-case asserts the exact stderr golden) so the `die` pattern delivers its message; `representable` also rejects a leading `-` (argparse reads a `-name` positional as an option unless a `--` separator precedes it, so such a dir is excluded rather than listed-but-`enable`-able only via `enable -- -name`) and C1 controls (0x7f-0x9f, not just C0+DEL).
- MoonBit navigation: use `rg` + `moon check`/`mooninfo`; core APIs live in `~/.moon/lib/core/<pkg>/pkg.generated.mbti` + READMEs.
- String scanning (M2.1 validator; no stdlib HTML parser) — call-forms below COMPILE (re-verified vs core/async mbti): scan in CODEPOINT space — `String::to_array()→Array[Char]`, index-walk, rebuild via `StringBuilder::new()` (an `#alias` of the `StringBuilder(size_hint?)` ctor) + `.write_char(Char)`/`.write_string(String)`/`.write_stringview(StringView)` (Logger-trait methods; `String::split(sep)` yields `Iter[StringView]` per state.mbt → `.to_owned()` a line to a `String`, or `write_stringview` to rejoin views) / `.to_string()` — dodges UTF-16 surrogate hazards on JA. `Char::to_int()→Int` + Unicode-range tests classify kanji/kana; `Int::to_char()→Char?` (invalid codepoint = None); `String::has_prefix(prefix : StringView)→Bool` (a string literal coerces). `@set` = `moonbitlang/core/set` = a MUTABLE hash set: `@set.Set(arr[:])` builds from an ArrayView (`#as_free_fn from_array`; `[][:]` = empty; `@set.Set([])` = empty when the LHS carries the type annotation `: @set.Set[String]`), `.add(x)→Unit` MUTATES in place (statement, no unused-value warn), `.contains(x)→Bool`, `.iter()→Iter[K]`. `get_attr` matches hyphenated attr names (`data-claim`) — `-`/`_`/`:`/`.` are all attr-name chars. ASCII case-fold = `+0x20` on codepoints A-Z (no alloc) → case-insensitive tag matching without lowering the whole string. IMPLEMENTATION: scan-in-`Array[Char]` + verbatim `StringBuilder` extraction over StringView slicing for interleaved structure/content work. ASYNC `@fs.exists(path)→Bool` + `@fs.remove(path)→Unit` (unlink a file) alongside the async `@fs.{…}` set above; `try @fs.kind(p) catch { _ => … } noraise { @fs.Directory => …, _ => … }` classifies dir-vs-file in one follow-stat.
- M2 validator (validate.mbt + provenance.mbt) durable lessons — code + git log hold the per-unit detail (roadmap compresses DONE units to a one-line marker); here = cross-cutting reuse for M2.3+/M3.
- 0-WARN TRAPS (drove every M2.1/2.2 split): a private fn or struct-field unused IN ITS OWN SLICE warns (whitebox-test use ≠ used — only `pub` or a real production caller silences); an empty `priv struct X {}` + literal `X::{}` are warn-free; an unconstructed `pub(all)` variant is warn-free → seed each split so its own primitives are self-using (a helper with a production caller, a struct with every field read in the same slice).
- CLEANED DOC = `clean_doc(html.to_array())`: ONE context-aware pass strips comments + blanks `<style>`/`<script>` bodies while copying ordinary tags atomically through quote-aware `skip_tag`; raw-text closing follows browser context, unterminated comment/raw text swallows/blanks to EOF. Attribute-embedded `<!--`/`<style>` decoys stay inert. Every tag walker gates markup on a real starter; stray text `<` advances one char, so it cannot swallow the next claim/image/frame tag. `find_data_claims(doc,start,end)` additionally enforces its half-open bound; `Elem.open_lt` includes an element's own `data-claim` in scoped coverage. Obligation scans decode numeric character references too — entity-encoded kanji cannot bypass ruby coverage.
- STATE-MACHINE well-formedness needs THREE guards, not one (general open/close-scanner bug-class): OPEN-arm (reject illegal nesting / strays), CLOSE-arm (primary check PLUS `|| inner_still_open` to catch an inner element left open), EOF (unclosed outer). A flag resolved ONLY in the close-arm silently misses an unclosed element.
- TAG-NAME BOUNDARY CONSISTENCY: every scanner starts on an ASCII letter, consumes the full token until structural space/`>`/`/`/end, then applies that same boundary. Thus `h2` depth-matches `</h2>`, while `<rt2>`/`<rt_foo>` remain distinct full names and cannot dispatch as `<rt>`.
- EXEMPTION-SURVIVES-UNCLOSED: an exemption from an OPEN state (`<code>`) must not outlive an unclosed container → track whether it hid a real obligation + re-assert at EOF. NON-VACUOUS PREDICATE: `all_kana("")` is vacuously true → pair an all-X check with an existence check.
- ONE-ROOT-CAUSE-ONE-FINDING: a structural COUNT check is the primary gate; a dependent TEXT/content check fires only in its `else` (when count==1), so a missing/duplicated slot fires ONLY the count code. `find_by_class` counts OUTERMOST slots (skips a matched body) → a same-class element nested inside another slot is uncounted, by design. M2.2b-ii's coverage scan REUSES this: `find_by_class(["entry","cap"])` skipping a matched subtree is SAFE because the template nests neither (`.entry`⊂`.col-text`, `.cap`⊂`.col-fig` = sibling columns). Codex (b-ii) raised nested-`.cap`-hidden (F2) + sibling-claim-hides-a-number (F1) as gaps; both REJECTED — element-level coverage (ANY `data-claim` in the element covers ITS numbers) is CORRECT for the model where an `.entry` = one claim across its jp/jpf/en translation tiers, and caps never nest in entries. Likewise `.png` match stays case-sensitive (F4 rejected) — the asset pipeline (`list_assets`/`check_imgs`) is exact-match throughout; generated bundles emit lowercase.
- CROSS-SLICE RECONCILIATION (GENERALIZABLE — hit at every stricter slice): a new check invalidates the prior slice's placeholder-data baseline → move prior baselines onto the new check's SKIP path (e.g. a provenance `gap` row skips source checks) or give them real passing data. THE recurring test-fix at each split.
- DE-RISK THE POSITIVE CONTRACT IN PYTHON FIRST (methodology, reuse for M2.4's first live bundle): mirror the string-scan design in a throwaway Python script, run it on `templates/deck.html`, confirm the real contract passes + each seeded defect isolates to ONE code, THEN write MoonBit — catches design holes (per-run vs per-char coverage) pre-implementation.
- REJECTED codex findings (reasoned — DON'T re-litigate at M2 review): 々〆〇 are NOT is_kanji (々 shares the preceding kanji's reading → a legit `<ruby>人<rt>ひと</rt></ruby>々` would false-fire coverage); ー・ are valid in readings (コーヒー); descendant (non-direct-child) tier/slide matching is INTENTIONAL (wrapper-descent); markup-in-`<rt>` can't fire on the template's plain-kana readings.
- PROVENANCE: `parse_src_ref` last-colon path + first-dash 1-based inclusive range; `line_slice` counts a trailing empty segment and rejects over-range; quote match is literal codepoint-space. Schema = nonempty id/claim + positive integer slide + status; gap omits evidence; IDs unique; deck/provenance ID sets exact in both directions and each ID stays on its declared slide; `no gaps` contradicts any gap row. Source boundary = lexical `normalize_path`/`is_inside` first, then canonical `realpath`; a canonical target may live under canonical src_base OR under the canonical root of its DIRECT symlinked sibling-project child, and never under canonical deck_home. Canonical final file is lstat-Regular before read. Residual = snapshot/TOCTOU without an `openat` handle chain.
- FIGURE reproducibility: live bundle scripts use Python stdlib to emit fixed SVG/HTML and installed `chromiumfish` to capture PNG; this is the repo's available authoring surface. A script depending on an undeclared historical environment (the old spine Matplotlib chart) is not reproducible even when its committed PNG exists. Run twice + compare hashes.
- MoonBit forms confirmed building the validator: optional labeled arg w/ default `label? : Type = default` (fmt rewrites `~=`→`?=`; bare `label~` = required-labeled), and an optional-arg default may CALL functions; `pub` silences unused even if only tests call it; a struct-literal push mutates a non-`mut` field (only reassignment needs `mut`); struct-literal field punning `Type::{a,b,c}`; `guard opt is Some(x) else {…}` = TOTAL unwrap (no panic); tag dispatch via a `(String, Bool)` tuple match; `continue` works in `for-in`. `@json`: construct via `Json::null()` etc. (bare variants read-only, Error 4036), pattern-MATCH variants freely. Test builders in `_test.mbt` (blackbox) are invisible to `_wbtest.mbt` (whitebox builds decks inline). Entity-decode is TOTAL (a bad `&` stays literal); `Int::to_char` returns None on surrogates or >0x10FFFF (boundary 0x10FFFF inclusive); `trim_ws` trims edge ASCII ws only (a decoded NBSP survives). MoonBit `print` is UNBOUND (only `println` is a global) → no-trailing-newline stdout = `@stdio.stdout.write(s)` (mirrors `@stdio.stderr.write`); `println(s)` on an `s` already ending in `\n` double-newlines, so a renderer that owns its trailing `\n` must be `write`n, not `println`d (codex-review M2.3a #6).
- TEMPLATE contract (hexdump-verified, reused by M2.4): 2 `<section class="slide">`, `.eyebrow .n`=`01`/`02`, `.pageno`=`project · N / 2` (separator = U+0020 U+00B7 U+0020, page nums UNpadded), slug=`project`; the shipped-template test asserts the FULL validator clean + a NEGATIVE-CONTROL proved it RUNS (not skips) via find_repo_root. Example claims are `example-verified` + `example-gap`; the whole-bundle test pins their exact deck↔provenance cross-reference.

## M3 render research (verified at plan)
- Chromium CLI capture is sufficient; no Playwright/CDP/image-crop surface needed. A stage-local iframe sized 2560×`count*1440`, shifted by `-index*1440`, captured rehab page 3 as exact 2560×1440 8-bit RGB and repeated byte-identically at explicit DSF=1. One wrapper/page avoids the prototype's unbounded tall PNG. Source names require file-URL encoding + HTML-attribute escaping. Browser stderr contains expected DBus/GPU initialization noise despite exit 0 + a valid artifact → gate on exit + lstat/IHDR, not empty stderr. `--dump-dom` is the browser-parsed/JS-executed DOM (per Chrome docs); feed it through the existing comment/raw-text-aware scanner rather than raw substring count.
- Raster-vs-vector proof: Chromium `--print-to-pdf --no-pdf-header-footer` on the six-page rehab deck emitted six 1920×1080-point pages + creation/modification timestamps because CSS fixes 1px=1/96in while PDF uses 1pt=1/72in. The chosen screenshot→img2pdf path preserves the 2560×1440 screen geometry. `img2pdf --nodate --engine=internal --imgsize=72dpi --rotation=none` over two repeated captures emitted byte-identical PDFs; `pdfinfo` = 2560×1440pt/page and `pdfimages` = original 2560×1440 RGB streams at 72dpi. This is deterministic on one fixed Chromium/font surface, not across browser/font upgrades.
- Render output belongs in ignored `renders/<project>/`, separate from the exact committed M2 bundle. Render consumes committed PNGs and never reruns `figures.py`; a source sibling may disappear without invalidating its committed deck. Reuse generation's no-follow stage/backup/live publication protocol via a generic extraction rather than duplicate it. A successful regeneration must invalidate that project's prior render before deck promotion; deletion before a later failed rename can lose a reproducible artifact but cannot leave a known-stale PDF.

## Env (verified)
- codex exec (model + effort per `~/.codex/config.toml` = `gpt-5.6-sol` + `max` effort; DELIBERATELY config-driven, NOT pinned in `build_codex_argv` — owner decision this session: keep the harness model/effort-agnostic (no `-m`/`-c` in-argv); `codex exec` inherits BOTH model+effort from active config (probe-verified). Accepted residual = those values ride MUTABLE external config (CODEX_HOME · user · project-`.codex` layers), not repo-pinned — if Sol/max ever a hard invariant, pin `-m` + `-c model_reasoning_effort=max` or a fail-closed preflight): `codex exec [PROMPT|stdin]` (stdin appended as `<stdin>` if both given). Flags: `-C/--cd` working root · `--add-dir` extra writable roots · `-s/--sandbox {read-only(DEFAULT),workspace-write,danger-full-access}` (exec DEFAULTS read-only → must raise to write) · `--dangerously-bypass-approvals-and-sandbox` (legit for externally-sandboxed hosts; exec has NO `-a/--ask-for-approval` — that's top-level `codex` only, exec REJECTS it exit 2, approval moot in non-interactive exec) · `--skip-git-repo-check` · `-o/--output-last-message` (final MSG only, NOT artifacts) · `--json` (JSONL; `turn.completed` vs `turn.failed`) · `--output-schema` (UNRELIABLE — silently dropped when tools/MCP active) · `-m/--model` · `-c key=value` (e.g. `-c model_reasoning_effort=low` PER-INVOCATION; config effort IS honored by `exec` (probe-verified) — the reset-to-medium report is interactive-TUI/mode-switch scoped, not `exec`) · `--ephemeral`. TRUST: exec shows NO trust prompt; `[projects."path"].trust_level` gates only whether project-local `.codex/` LOADS, not runtime → explicit `--sandbox` runs ANY dir (VERIFIED M2 plan: read-only exec on a non-trusted sibling → exit 0, replied). RELIABILITY: success = exit 0 AND the artifact exists/non-empty/validates (exit 0 can still truncate → ALWAYS validate the file, not just the code); codex has no idempotency + writes a MULTI-FILE bundle → stage into a temp bundle DIR, validate the WHOLE dir, then promote (rename dir, replace existing) — stage under a RESERVED dot-dir `decks/.partial/<project>` NOT `decks/<project>.partial` (the latter collides with a sibling literally named `<project>.partial`; `representable` allows internal dots, but `discover` skips leading-dot names so the dot-dir is never a candidate, making the scheme provably collision-free — codex-review M2.3a); retry-once; wrap in external `timeout --kill-after=<grace> <secs>` (plain `timeout <secs>` only SIGTERMs → a signal-ignoring run hangs `collect_output` forever; `--kill-after` SIGKILLs after the grace, M2.3a uses 30s; `timeout 0 <cmd>` DISABLES the timeout entirely (GNU) → the live ceiling stays positive; only per-provider retries exist). Bypass = NO write confinement. Every producer/postprocessor exit is checked before publication against a per-project `GitSnapshot` (porcelain-v1/NUL + binary `HEAD`→worktree diff): all tracked paths, including already-dirty content and published decks; any change is non-retryable. This defense-in-depth still misses sibling repos, untracked/ignored/non-git paths, and writes outside the repo; it is absent when either Git snapshot fails. Canonical provenance confinement remains the anti-hallucination boundary. Publication requires real no-follow `decks/`, `decks/.partial/`, and `decks/.previous/` roots, stages in `.partial/`, and preserves the old deck in `.previous/`; ordinary failures roll back and an interrupted swap recovers on the next run, while the portable two-rename crash interval remains. COST: ~18K tokens for a trivial probe → real deck generation far heavier. → M2 has codex WRITE the bundle to fixed paths; final message = a manifest. M2.4a now runtime-loads `templates/deck-prompt.md`, embeds the complete HTML contract per invocation, and makes dry-run async/runtime-loaded; the first live `spine-segmentation` run passed whole-bundle validation.
- furigana (JA kanji→kana reading) tooling (2026 research): frontier LLM inline ≈0.5% kana-CER (best overall) but silently emits fluent WRONG kana; deterministic analyzers UniDic≈1.5% / Sudachi≈1.8% CER, worse on technical/proper-noun/loanword vocab + emit one best path with no ambiguity signal. HYBRID (LLM draft + analyzer cross-read, disagreements→human review) = SOTA honest approach; no dedicated agent furigana tool exists. Cheap deterministic honesty guards (no analyzer, MoonBit-checkable): strip `<rt>` from `.jpf` → residue must == the `.jp` tier (CROSS-TIER consistency: catches a kanji dropped/substituted between tiers, NOT source-truth); `<rt>` all-kana; ruby coverage (WITHIN `.jpf`) is PER-KANJI-CHARACTER (every kanji sits in some ruby base, NOT one-ruby-per-run — a kanji run may split across adjacent ruby groups, e.g. 要点一 as 要点+一, which a run-based check false-fails). M2.5 selected Fugashi 1.5.2 + `unidic-lite` (47MB sdist, ~250MB installed; full current UniDic needs a separate ~770MB download) as a uv-locked AUTHORING SIDECAR: `uv run --locked --no-dev python tools/furigana_check.py <bundle>`. Explicitly bind `unidic_lite.DICDIR` and use `feature.kana` (orthographic reading); normalize katakana→hiragana, then align whole-token readings back to ruby spans through known-kana anchors (handles split ruby + okurigana). Ambiguous splits/OOV + analyzer disagreements become idempotent `gaps.md` entries between reserved markers. Pipeline no-follow-checks + tripwires + validates raw Codex output BEFORE the sidecar, then repeats the guards AFTER it; the sidecar rejects non-regular/symlink inputs and atomically replaces gaps → partial bundles retain precise validator codes while direct stage-root/mandatory-file write-through is blocked. Remaining harness limits are recorded in roadmap M2 review closure. Python/UniDic stay external to the MoonBit binary.
- chromiumfish (headless Chromium via playwright-core) + Noto Sans CJK JP both present → render stack ready for M3. `img2pdf` installed via `uv tool` this session (alternative to PIL for the PDF step).
